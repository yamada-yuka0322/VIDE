/*
  File autogenerated by gengetopt version 2.22.5
  generated with the following command:
  ../../ext_install/bin/gengetopt -i /mnt/data/yuka/repository/vide_public/c_tools/mock/generateMock.ggo -f generateMock_conf -a generateMock_info -F generateMock_conf -C zobovConf_info 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef FIX_UNUSED
#define FIX_UNUSED(X) (void) (X) /* avoid warnings for unused params */
#endif

#include <getopt.h>

#include "generateMock_conf.h"

const char *generateMock_info_purpose = "";

const char *generateMock_info_usage = "Usage: generateMock [OPTIONS]...";

const char *generateMock_info_description = "";

const char *generateMock_info_help[] = {
  "  -h, --help                    Print help and exit",
  "  -V, --version                 Print version and exit",
  "      --configFile=STRING       Configuration file path",
  "      --ramsesBase=STRING       Base directory for ramses",
  "      --ramsesId=INT            Ramses snapshot id",
  "      --gadget=STRING           Base name of gadget snapshot (without parallel \n                                  writing extension)",
  "      --gadget2=STRING          Base name of gadget snapshot (version 2, \n                                  without parallel writing extension)",
  "      --flash=STRING            Base name for FLASH snapshot",
  "      --multidark=STRING        Base name for multidark snapshot",
  "      --sdf=STRING              SDF snapshot name",
  "      --sdf_splitting=INT       Number of artificial splitting of the SDF \n                                  snapshot  (default=`20')",
  "      --axis=INT                Redshift axis (X=0, Y=1, Z=2)  (default=`2')",
  "      --output=STRING           Output filename for particles",
  "      --outputParameter=STRING  Output geometry parameter file for \n                                  postprocessing",
  "      --rangeX_min=DOUBLE       Minimum range in X for making the box",
  "      --rangeX_max=DOUBLE       Maximum range in X for making the box",
  "      --rangeY_min=DOUBLE       Minimum range in Y for making the box",
  "      --rangeY_max=DOUBLE       Maximum range in Y for making the box",
  "      --rangeZ_min=DOUBLE       Minimum range in Z for making the box (after \n                                  distortion)",
  "      --rangeZ_max=DOUBLE       Maximum range in Z for making the box (after \n                                  distortion)",
  "      --preReShift              Reshift the zero of the Z axis  (default=off)",
  "      --peculiarVelocities      Added peculiar velocities distortion  \n                                  (default=off)",
  "      --cosmo                   Apply cosmological redshift  (default=off)",
  "      --subsample=DOUBLE        Subsample the input simulation by the specified \n                                  amount  (default=`1.0')",
  "      --inputParameter=STRING   Input geometry (optional, warning!)",
  "      --gadgetUnit=DOUBLE       Unit of length in gadget file in Mpc/h  \n                                  (default=`0.001')",
  "      --subsample_seed=INT      Seed for random number generation to select the \n                                  subsample  (default=`190524')",
  "      --resubsample=DOUBLE      Resubsampling factor compared to the subsampled \n                                  simulation",
  "      --resubsample_seed=INT    Seed for resubsampling from a subsampled \n                                  simulation  (default=`20132011')",
  "      --joggleParticles         Slightly joggle the input particle positions  \n                                  (default=off)",
    0
};

typedef enum {ARG_NO
  , ARG_FLAG
  , ARG_STRING
  , ARG_INT
  , ARG_DOUBLE
} generateMock_conf_arg_type;

static
void clear_given (struct generateMock_info *args_info);
static
void clear_args (struct generateMock_info *args_info);

static int
generateMock_conf_internal (int argc, char **argv, struct generateMock_info *args_info,
                        struct generateMock_conf_params *params, const char *additional_error);

static int
generateMock_conf_required2 (struct generateMock_info *args_info, const char *prog_name, const char *additional_error);
struct line_list
{
  char * string_arg;
  struct line_list * next;
};

static struct line_list *cmd_line_list = 0;
static struct line_list *cmd_line_list_tmp = 0;

static void
free_cmd_list(void)
{
  /* free the list of a previous call */
  if (cmd_line_list)
    {
      while (cmd_line_list) {
        cmd_line_list_tmp = cmd_line_list;
        cmd_line_list = cmd_line_list->next;
        free (cmd_line_list_tmp->string_arg);
        free (cmd_line_list_tmp);
      }
    }
}


static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct generateMock_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->configFile_given = 0 ;
  args_info->ramsesBase_given = 0 ;
  args_info->ramsesId_given = 0 ;
  args_info->gadget_given = 0 ;
  args_info->gadget2_given = 0 ;
  args_info->flash_given = 0 ;
  args_info->multidark_given = 0 ;
  args_info->sdf_given = 0 ;
  args_info->sdf_splitting_given = 0 ;
  args_info->axis_given = 0 ;
  args_info->output_given = 0 ;
  args_info->outputParameter_given = 0 ;
  args_info->rangeX_min_given = 0 ;
  args_info->rangeX_max_given = 0 ;
  args_info->rangeY_min_given = 0 ;
  args_info->rangeY_max_given = 0 ;
  args_info->rangeZ_min_given = 0 ;
  args_info->rangeZ_max_given = 0 ;
  args_info->preReShift_given = 0 ;
  args_info->peculiarVelocities_given = 0 ;
  args_info->cosmo_given = 0 ;
  args_info->subsample_given = 0 ;
  args_info->inputParameter_given = 0 ;
  args_info->gadgetUnit_given = 0 ;
  args_info->subsample_seed_given = 0 ;
  args_info->resubsample_given = 0 ;
  args_info->resubsample_seed_given = 0 ;
  args_info->joggleParticles_given = 0 ;
}

static
void clear_args (struct generateMock_info *args_info)
{
  FIX_UNUSED (args_info);
  args_info->configFile_arg = NULL;
  args_info->configFile_orig = NULL;
  args_info->ramsesBase_arg = NULL;
  args_info->ramsesBase_orig = NULL;
  args_info->ramsesId_orig = NULL;
  args_info->gadget_arg = NULL;
  args_info->gadget_orig = NULL;
  args_info->gadget2_arg = NULL;
  args_info->gadget2_orig = NULL;
  args_info->flash_arg = NULL;
  args_info->flash_orig = NULL;
  args_info->multidark_arg = NULL;
  args_info->multidark_orig = NULL;
  args_info->sdf_arg = NULL;
  args_info->sdf_orig = NULL;
  args_info->sdf_splitting_arg = 20;
  args_info->sdf_splitting_orig = NULL;
  args_info->axis_arg = 2;
  args_info->axis_orig = NULL;
  args_info->output_arg = NULL;
  args_info->output_orig = NULL;
  args_info->outputParameter_arg = NULL;
  args_info->outputParameter_orig = NULL;
  args_info->rangeX_min_orig = NULL;
  args_info->rangeX_max_orig = NULL;
  args_info->rangeY_min_orig = NULL;
  args_info->rangeY_max_orig = NULL;
  args_info->rangeZ_min_orig = NULL;
  args_info->rangeZ_max_orig = NULL;
  args_info->preReShift_flag = 0;
  args_info->peculiarVelocities_flag = 0;
  args_info->cosmo_flag = 0;
  args_info->subsample_arg = 1.0;
  args_info->subsample_orig = NULL;
  args_info->inputParameter_arg = NULL;
  args_info->inputParameter_orig = NULL;
  args_info->gadgetUnit_arg = 0.001;
  args_info->gadgetUnit_orig = NULL;
  args_info->subsample_seed_arg = 190524;
  args_info->subsample_seed_orig = NULL;
  args_info->resubsample_orig = NULL;
  args_info->resubsample_seed_arg = 20132011;
  args_info->resubsample_seed_orig = NULL;
  args_info->joggleParticles_flag = 0;
  
}

static
void init_args_info(struct generateMock_info *args_info)
{


  args_info->help_help = generateMock_info_help[0] ;
  args_info->version_help = generateMock_info_help[1] ;
  args_info->configFile_help = generateMock_info_help[2] ;
  args_info->ramsesBase_help = generateMock_info_help[3] ;
  args_info->ramsesId_help = generateMock_info_help[4] ;
  args_info->gadget_help = generateMock_info_help[5] ;
  args_info->gadget2_help = generateMock_info_help[6] ;
  args_info->flash_help = generateMock_info_help[7] ;
  args_info->multidark_help = generateMock_info_help[8] ;
  args_info->sdf_help = generateMock_info_help[9] ;
  args_info->sdf_splitting_help = generateMock_info_help[10] ;
  args_info->axis_help = generateMock_info_help[11] ;
  args_info->output_help = generateMock_info_help[12] ;
  args_info->outputParameter_help = generateMock_info_help[13] ;
  args_info->rangeX_min_help = generateMock_info_help[14] ;
  args_info->rangeX_max_help = generateMock_info_help[15] ;
  args_info->rangeY_min_help = generateMock_info_help[16] ;
  args_info->rangeY_max_help = generateMock_info_help[17] ;
  args_info->rangeZ_min_help = generateMock_info_help[18] ;
  args_info->rangeZ_max_help = generateMock_info_help[19] ;
  args_info->preReShift_help = generateMock_info_help[20] ;
  args_info->peculiarVelocities_help = generateMock_info_help[21] ;
  args_info->cosmo_help = generateMock_info_help[22] ;
  args_info->subsample_help = generateMock_info_help[23] ;
  args_info->inputParameter_help = generateMock_info_help[24] ;
  args_info->gadgetUnit_help = generateMock_info_help[25] ;
  args_info->subsample_seed_help = generateMock_info_help[26] ;
  args_info->resubsample_help = generateMock_info_help[27] ;
  args_info->resubsample_seed_help = generateMock_info_help[28] ;
  args_info->joggleParticles_help = generateMock_info_help[29] ;
  
}

void
generateMock_conf_print_version (void)
{
  printf ("%s %s\n",
     (strlen(GENERATEMOCK_CONF_PACKAGE_NAME) ? GENERATEMOCK_CONF_PACKAGE_NAME : GENERATEMOCK_CONF_PACKAGE),
     GENERATEMOCK_CONF_VERSION);
}

static void print_help_common(void) {
  generateMock_conf_print_version ();

  if (strlen(generateMock_info_purpose) > 0)
    printf("\n%s\n", generateMock_info_purpose);

  if (strlen(generateMock_info_usage) > 0)
    printf("\n%s\n", generateMock_info_usage);

  printf("\n");

  if (strlen(generateMock_info_description) > 0)
    printf("%s\n\n", generateMock_info_description);
}

void
generateMock_conf_print_help (void)
{
  int i = 0;
  print_help_common();
  while (generateMock_info_help[i])
    printf("%s\n", generateMock_info_help[i++]);
}

void
generateMock_conf_init (struct generateMock_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);
}

void
generateMock_conf_params_init(struct generateMock_conf_params *params)
{
  if (params)
    { 
      params->override = 0;
      params->initialize = 1;
      params->check_required = 1;
      params->check_ambiguity = 0;
      params->print_errors = 1;
    }
}

struct generateMock_conf_params *
generateMock_conf_params_create(void)
{
  struct generateMock_conf_params *params = 
    (struct generateMock_conf_params *)malloc(sizeof(struct generateMock_conf_params));
  generateMock_conf_params_init(params);  
  return params;
}

static void
free_string_field (char **s)
{
  if (*s)
    {
      free (*s);
      *s = 0;
    }
}


static void
generateMock_conf_release (struct generateMock_info *args_info)
{

  free_string_field (&(args_info->configFile_arg));
  free_string_field (&(args_info->configFile_orig));
  free_string_field (&(args_info->ramsesBase_arg));
  free_string_field (&(args_info->ramsesBase_orig));
  free_string_field (&(args_info->ramsesId_orig));
  free_string_field (&(args_info->gadget_arg));
  free_string_field (&(args_info->gadget_orig));
  free_string_field (&(args_info->gadget2_arg));
  free_string_field (&(args_info->gadget2_orig));
  free_string_field (&(args_info->flash_arg));
  free_string_field (&(args_info->flash_orig));
  free_string_field (&(args_info->multidark_arg));
  free_string_field (&(args_info->multidark_orig));
  free_string_field (&(args_info->sdf_arg));
  free_string_field (&(args_info->sdf_orig));
  free_string_field (&(args_info->sdf_splitting_orig));
  free_string_field (&(args_info->axis_orig));
  free_string_field (&(args_info->output_arg));
  free_string_field (&(args_info->output_orig));
  free_string_field (&(args_info->outputParameter_arg));
  free_string_field (&(args_info->outputParameter_orig));
  free_string_field (&(args_info->rangeX_min_orig));
  free_string_field (&(args_info->rangeX_max_orig));
  free_string_field (&(args_info->rangeY_min_orig));
  free_string_field (&(args_info->rangeY_max_orig));
  free_string_field (&(args_info->rangeZ_min_orig));
  free_string_field (&(args_info->rangeZ_max_orig));
  free_string_field (&(args_info->subsample_orig));
  free_string_field (&(args_info->inputParameter_arg));
  free_string_field (&(args_info->inputParameter_orig));
  free_string_field (&(args_info->gadgetUnit_orig));
  free_string_field (&(args_info->subsample_seed_orig));
  free_string_field (&(args_info->resubsample_orig));
  free_string_field (&(args_info->resubsample_seed_orig));
  
  

  clear_given (args_info);
}


static void
write_into_file(FILE *outfile, const char *opt, const char *arg, const char *values[])
{
  FIX_UNUSED (values);
  if (arg) {
    fprintf(outfile, "%s=\"%s\"\n", opt, arg);
  } else {
    fprintf(outfile, "%s\n", opt);
  }
}


int
generateMock_conf_dump(FILE *outfile, struct generateMock_info *args_info)
{
  int i = 0;

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot dump options to stream\n", GENERATEMOCK_CONF_PACKAGE);
      return EXIT_FAILURE;
    }

  if (args_info->help_given)
    write_into_file(outfile, "help", 0, 0 );
  if (args_info->version_given)
    write_into_file(outfile, "version", 0, 0 );
  if (args_info->configFile_given)
    write_into_file(outfile, "configFile", args_info->configFile_orig, 0);
  if (args_info->ramsesBase_given)
    write_into_file(outfile, "ramsesBase", args_info->ramsesBase_orig, 0);
  if (args_info->ramsesId_given)
    write_into_file(outfile, "ramsesId", args_info->ramsesId_orig, 0);
  if (args_info->gadget_given)
    write_into_file(outfile, "gadget", args_info->gadget_orig, 0);
  if (args_info->gadget2_given)
    write_into_file(outfile, "gadget2", args_info->gadget2_orig, 0);
  if (args_info->flash_given)
    write_into_file(outfile, "flash", args_info->flash_orig, 0);
  if (args_info->multidark_given)
    write_into_file(outfile, "multidark", args_info->multidark_orig, 0);
  if (args_info->sdf_given)
    write_into_file(outfile, "sdf", args_info->sdf_orig, 0);
  if (args_info->sdf_splitting_given)
    write_into_file(outfile, "sdf_splitting", args_info->sdf_splitting_orig, 0);
  if (args_info->axis_given)
    write_into_file(outfile, "axis", args_info->axis_orig, 0);
  if (args_info->output_given)
    write_into_file(outfile, "output", args_info->output_orig, 0);
  if (args_info->outputParameter_given)
    write_into_file(outfile, "outputParameter", args_info->outputParameter_orig, 0);
  if (args_info->rangeX_min_given)
    write_into_file(outfile, "rangeX_min", args_info->rangeX_min_orig, 0);
  if (args_info->rangeX_max_given)
    write_into_file(outfile, "rangeX_max", args_info->rangeX_max_orig, 0);
  if (args_info->rangeY_min_given)
    write_into_file(outfile, "rangeY_min", args_info->rangeY_min_orig, 0);
  if (args_info->rangeY_max_given)
    write_into_file(outfile, "rangeY_max", args_info->rangeY_max_orig, 0);
  if (args_info->rangeZ_min_given)
    write_into_file(outfile, "rangeZ_min", args_info->rangeZ_min_orig, 0);
  if (args_info->rangeZ_max_given)
    write_into_file(outfile, "rangeZ_max", args_info->rangeZ_max_orig, 0);
  if (args_info->preReShift_given)
    write_into_file(outfile, "preReShift", 0, 0 );
  if (args_info->peculiarVelocities_given)
    write_into_file(outfile, "peculiarVelocities", 0, 0 );
  if (args_info->cosmo_given)
    write_into_file(outfile, "cosmo", 0, 0 );
  if (args_info->subsample_given)
    write_into_file(outfile, "subsample", args_info->subsample_orig, 0);
  if (args_info->inputParameter_given)
    write_into_file(outfile, "inputParameter", args_info->inputParameter_orig, 0);
  if (args_info->gadgetUnit_given)
    write_into_file(outfile, "gadgetUnit", args_info->gadgetUnit_orig, 0);
  if (args_info->subsample_seed_given)
    write_into_file(outfile, "subsample_seed", args_info->subsample_seed_orig, 0);
  if (args_info->resubsample_given)
    write_into_file(outfile, "resubsample", args_info->resubsample_orig, 0);
  if (args_info->resubsample_seed_given)
    write_into_file(outfile, "resubsample_seed", args_info->resubsample_seed_orig, 0);
  if (args_info->joggleParticles_given)
    write_into_file(outfile, "joggleParticles", 0, 0 );
  

  i = EXIT_SUCCESS;
  return i;
}

int
generateMock_conf_file_save(const char *filename, struct generateMock_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", GENERATEMOCK_CONF_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  i = generateMock_conf_dump(outfile, args_info);
  fclose (outfile);

  return i;
}

void
generateMock_conf_free (struct generateMock_info *args_info)
{
  generateMock_conf_release (args_info);
}

/** @brief replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = 0;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
generateMock_conf (int argc, char **argv, struct generateMock_info *args_info)
{
  return generateMock_conf2 (argc, argv, args_info, 0, 1, 1);
}

int
generateMock_conf_ext (int argc, char **argv, struct generateMock_info *args_info,
                   struct generateMock_conf_params *params)
{
  int result;
  result = generateMock_conf_internal (argc, argv, args_info, params, 0);

  if (result == EXIT_FAILURE)
    {
      generateMock_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
generateMock_conf2 (int argc, char **argv, struct generateMock_info *args_info, int override, int initialize, int check_required)
{
  int result;
  struct generateMock_conf_params params;
  
  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;

  result = generateMock_conf_internal (argc, argv, args_info, &params, 0);

  if (result == EXIT_FAILURE)
    {
      generateMock_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
generateMock_conf_required (struct generateMock_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (generateMock_conf_required2(args_info, prog_name, 0) > 0)
    result = EXIT_FAILURE;

  if (result == EXIT_FAILURE)
    {
      generateMock_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
generateMock_conf_required2 (struct generateMock_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;
  FIX_UNUSED (additional_error);

  /* checks for required options */
  if (! args_info->output_given)
    {
      fprintf (stderr, "%s: '--output' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->outputParameter_given)
    {
      fprintf (stderr, "%s: '--outputParameter' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->rangeX_min_given)
    {
      fprintf (stderr, "%s: '--rangeX_min' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->rangeX_max_given)
    {
      fprintf (stderr, "%s: '--rangeX_max' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->rangeY_min_given)
    {
      fprintf (stderr, "%s: '--rangeY_min' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->rangeY_max_given)
    {
      fprintf (stderr, "%s: '--rangeY_max' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->rangeZ_min_given)
    {
      fprintf (stderr, "%s: '--rangeZ_min' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->rangeZ_max_given)
    {
      fprintf (stderr, "%s: '--rangeZ_max' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  
  /* checks for dependences among options */

  return error;
}


static char *package_name = 0;

/**
 * @brief updates an option
 * @param field the generic pointer to the field to update
 * @param orig_field the pointer to the orig field
 * @param field_given the pointer to the number of occurrence of this option
 * @param prev_given the pointer to the number of occurrence already seen
 * @param value the argument for this option (if null no arg was specified)
 * @param possible_values the possible values for this option (if specified)
 * @param default_value the default value (in case the option only accepts fixed values)
 * @param arg_type the type of this option
 * @param check_ambiguity @see generateMock_conf_params.check_ambiguity
 * @param override @see generateMock_conf_params.override
 * @param no_free whether to free a possible previous value
 * @param multiple_option whether this is a multiple option
 * @param long_opt the corresponding long option
 * @param short_opt the corresponding short option (or '-' if none)
 * @param additional_error possible further error specification
 */
static
int update_arg(void *field, char **orig_field,
               unsigned int *field_given, unsigned int *prev_given, 
               char *value, const char *possible_values[],
               const char *default_value,
               generateMock_conf_arg_type arg_type,
               int check_ambiguity, int override,
               int no_free, int multiple_option,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  char *stop_char = 0;
  const char *val = value;
  int found;
  char **string_field;
  FIX_UNUSED (field);

  stop_char = 0;
  found = 0;

  if (!multiple_option && prev_given && (*prev_given || (check_ambiguity && *field_given)))
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: `--%s' (`-%c') option given more than once%s\n", 
               package_name, long_opt, short_opt,
               (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: `--%s' option given more than once%s\n", 
               package_name, long_opt,
               (additional_error ? additional_error : ""));
      return 1; /* failure */
    }

  FIX_UNUSED (default_value);
    
  if (field_given && *field_given && ! override)
    return 0;
  if (prev_given)
    (*prev_given)++;
  if (field_given)
    (*field_given)++;
  if (possible_values)
    val = possible_values[found];

  switch(arg_type) {
  case ARG_FLAG:
    *((int *)field) = !*((int *)field);
    break;
  case ARG_INT:
    if (val) *((int *)field) = strtol (val, &stop_char, 0);
    break;
  case ARG_DOUBLE:
    if (val) *((double *)field) = strtod (val, &stop_char);
    break;
  case ARG_STRING:
    if (val) {
      string_field = (char **)field;
      if (!no_free && *string_field)
        free (*string_field); /* free previous string */
      *string_field = gengetopt_strdup (val);
    }
    break;
  default:
    break;
  };

  /* check numeric conversion */
  switch(arg_type) {
  case ARG_INT:
  case ARG_DOUBLE:
    if (val && !(stop_char && *stop_char == '\0')) {
      fprintf(stderr, "%s: invalid numeric value: %s\n", package_name, val);
      return 1; /* failure */
    }
    break;
  default:
    ;
  };

  /* store the original value */
  switch(arg_type) {
  case ARG_NO:
  case ARG_FLAG:
    break;
  default:
    if (value && orig_field) {
      if (no_free) {
        *orig_field = value;
      } else {
        if (*orig_field)
          free (*orig_field); /* free previous string */
        *orig_field = gengetopt_strdup (value);
      }
    }
  };

  return 0; /* OK */
}


int
generateMock_conf_internal (
  int argc, char **argv, struct generateMock_info *args_info,
                        struct generateMock_conf_params *params, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct generateMock_info local_args_info;
  
  int override;
  int initialize;
  int check_required;
  int check_ambiguity;
  
  package_name = argv[0];
  
  override = params->override;
  initialize = params->initialize;
  check_required = params->check_required;
  check_ambiguity = params->check_ambiguity;

  if (initialize)
    generateMock_conf_init (args_info);

  generateMock_conf_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = params->print_errors;
  optopt = '?';

  while (1)
    {
      int option_index = 0;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "configFile",	1, NULL, 0 },
        { "ramsesBase",	1, NULL, 0 },
        { "ramsesId",	1, NULL, 0 },
        { "gadget",	1, NULL, 0 },
        { "gadget2",	1, NULL, 0 },
        { "flash",	1, NULL, 0 },
        { "multidark",	1, NULL, 0 },
        { "sdf",	1, NULL, 0 },
        { "sdf_splitting",	1, NULL, 0 },
        { "axis",	1, NULL, 0 },
        { "output",	1, NULL, 0 },
        { "outputParameter",	1, NULL, 0 },
        { "rangeX_min",	1, NULL, 0 },
        { "rangeX_max",	1, NULL, 0 },
        { "rangeY_min",	1, NULL, 0 },
        { "rangeY_max",	1, NULL, 0 },
        { "rangeZ_min",	1, NULL, 0 },
        { "rangeZ_max",	1, NULL, 0 },
        { "preReShift",	0, NULL, 0 },
        { "peculiarVelocities",	0, NULL, 0 },
        { "cosmo",	0, NULL, 0 },
        { "subsample",	1, NULL, 0 },
        { "inputParameter",	1, NULL, 0 },
        { "gadgetUnit",	1, NULL, 0 },
        { "subsample_seed",	1, NULL, 0 },
        { "resubsample",	1, NULL, 0 },
        { "resubsample_seed",	1, NULL, 0 },
        { "joggleParticles",	0, NULL, 0 },
        { 0,  0, 0, 0 }
      };

      c = getopt_long (argc, argv, "hV", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          generateMock_conf_print_help ();
          generateMock_conf_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          generateMock_conf_print_version ();
          generateMock_conf_free (&local_args_info);
          exit (EXIT_SUCCESS);


        case 0:	/* Long option with no short option */
          /* Configuration file path.  */
          if (strcmp (long_options[option_index].name, "configFile") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->configFile_arg), 
                 &(args_info->configFile_orig), &(args_info->configFile_given),
                &(local_args_info.configFile_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "configFile", '-',
                additional_error))
              goto failure;
          
          }
          /* Base directory for ramses.  */
          else if (strcmp (long_options[option_index].name, "ramsesBase") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->ramsesBase_arg), 
                 &(args_info->ramsesBase_orig), &(args_info->ramsesBase_given),
                &(local_args_info.ramsesBase_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "ramsesBase", '-',
                additional_error))
              goto failure;
          
          }
          /* Ramses snapshot id.  */
          else if (strcmp (long_options[option_index].name, "ramsesId") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->ramsesId_arg), 
                 &(args_info->ramsesId_orig), &(args_info->ramsesId_given),
                &(local_args_info.ramsesId_given), optarg, 0, 0, ARG_INT,
                check_ambiguity, override, 0, 0,
                "ramsesId", '-',
                additional_error))
              goto failure;
          
          }
          /* Base name of gadget snapshot (without parallel writing extension).  */
          else if (strcmp (long_options[option_index].name, "gadget") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->gadget_arg), 
                 &(args_info->gadget_orig), &(args_info->gadget_given),
                &(local_args_info.gadget_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "gadget", '-',
                additional_error))
              goto failure;
          
          }
          /* Base name of gadget snapshot (version 2, without parallel writing extension).  */
          else if (strcmp (long_options[option_index].name, "gadget2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->gadget2_arg), 
                 &(args_info->gadget2_orig), &(args_info->gadget2_given),
                &(local_args_info.gadget2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "gadget2", '-',
                additional_error))
              goto failure;
          
          }
          /* Base name for FLASH snapshot.  */
          else if (strcmp (long_options[option_index].name, "flash") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->flash_arg), 
                 &(args_info->flash_orig), &(args_info->flash_given),
                &(local_args_info.flash_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "flash", '-',
                additional_error))
              goto failure;
          
          }
          /* Base name for multidark snapshot.  */
          else if (strcmp (long_options[option_index].name, "multidark") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->multidark_arg), 
                 &(args_info->multidark_orig), &(args_info->multidark_given),
                &(local_args_info.multidark_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "multidark", '-',
                additional_error))
              goto failure;
          
          }
          /* SDF snapshot name.  */
          else if (strcmp (long_options[option_index].name, "sdf") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->sdf_arg), 
                 &(args_info->sdf_orig), &(args_info->sdf_given),
                &(local_args_info.sdf_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "sdf", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of artificial splitting of the SDF snapshot.  */
          else if (strcmp (long_options[option_index].name, "sdf_splitting") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->sdf_splitting_arg), 
                 &(args_info->sdf_splitting_orig), &(args_info->sdf_splitting_given),
                &(local_args_info.sdf_splitting_given), optarg, 0, "20", ARG_INT,
                check_ambiguity, override, 0, 0,
                "sdf_splitting", '-',
                additional_error))
              goto failure;
          
          }
          /* Redshift axis (X=0, Y=1, Z=2).  */
          else if (strcmp (long_options[option_index].name, "axis") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->axis_arg), 
                 &(args_info->axis_orig), &(args_info->axis_given),
                &(local_args_info.axis_given), optarg, 0, "2", ARG_INT,
                check_ambiguity, override, 0, 0,
                "axis", '-',
                additional_error))
              goto failure;
          
          }
          /* Output filename for particles.  */
          else if (strcmp (long_options[option_index].name, "output") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->output_arg), 
                 &(args_info->output_orig), &(args_info->output_given),
                &(local_args_info.output_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "output", '-',
                additional_error))
              goto failure;
          
          }
          /* Output geometry parameter file for postprocessing.  */
          else if (strcmp (long_options[option_index].name, "outputParameter") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->outputParameter_arg), 
                 &(args_info->outputParameter_orig), &(args_info->outputParameter_given),
                &(local_args_info.outputParameter_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "outputParameter", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum range in X for making the box.  */
          else if (strcmp (long_options[option_index].name, "rangeX_min") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->rangeX_min_arg), 
                 &(args_info->rangeX_min_orig), &(args_info->rangeX_min_given),
                &(local_args_info.rangeX_min_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "rangeX_min", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum range in X for making the box.  */
          else if (strcmp (long_options[option_index].name, "rangeX_max") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->rangeX_max_arg), 
                 &(args_info->rangeX_max_orig), &(args_info->rangeX_max_given),
                &(local_args_info.rangeX_max_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "rangeX_max", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum range in Y for making the box.  */
          else if (strcmp (long_options[option_index].name, "rangeY_min") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->rangeY_min_arg), 
                 &(args_info->rangeY_min_orig), &(args_info->rangeY_min_given),
                &(local_args_info.rangeY_min_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "rangeY_min", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum range in Y for making the box.  */
          else if (strcmp (long_options[option_index].name, "rangeY_max") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->rangeY_max_arg), 
                 &(args_info->rangeY_max_orig), &(args_info->rangeY_max_given),
                &(local_args_info.rangeY_max_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "rangeY_max", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum range in Z for making the box (after distortion).  */
          else if (strcmp (long_options[option_index].name, "rangeZ_min") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->rangeZ_min_arg), 
                 &(args_info->rangeZ_min_orig), &(args_info->rangeZ_min_given),
                &(local_args_info.rangeZ_min_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "rangeZ_min", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum range in Z for making the box (after distortion).  */
          else if (strcmp (long_options[option_index].name, "rangeZ_max") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->rangeZ_max_arg), 
                 &(args_info->rangeZ_max_orig), &(args_info->rangeZ_max_given),
                &(local_args_info.rangeZ_max_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "rangeZ_max", '-',
                additional_error))
              goto failure;
          
          }
          /* Reshift the zero of the Z axis.  */
          else if (strcmp (long_options[option_index].name, "preReShift") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->preReShift_flag), 0, &(args_info->preReShift_given),
                &(local_args_info.preReShift_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "preReShift", '-',
                additional_error))
              goto failure;
          
          }
          /* Added peculiar velocities distortion.  */
          else if (strcmp (long_options[option_index].name, "peculiarVelocities") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->peculiarVelocities_flag), 0, &(args_info->peculiarVelocities_given),
                &(local_args_info.peculiarVelocities_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "peculiarVelocities", '-',
                additional_error))
              goto failure;
          
          }
          /* Apply cosmological redshift.  */
          else if (strcmp (long_options[option_index].name, "cosmo") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->cosmo_flag), 0, &(args_info->cosmo_given),
                &(local_args_info.cosmo_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "cosmo", '-',
                additional_error))
              goto failure;
          
          }
          /* Subsample the input simulation by the specified amount.  */
          else if (strcmp (long_options[option_index].name, "subsample") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->subsample_arg), 
                 &(args_info->subsample_orig), &(args_info->subsample_given),
                &(local_args_info.subsample_given), optarg, 0, "1.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "subsample", '-',
                additional_error))
              goto failure;
          
          }
          /* Input geometry (optional, warning!).  */
          else if (strcmp (long_options[option_index].name, "inputParameter") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->inputParameter_arg), 
                 &(args_info->inputParameter_orig), &(args_info->inputParameter_given),
                &(local_args_info.inputParameter_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "inputParameter", '-',
                additional_error))
              goto failure;
          
          }
          /* Unit of length in gadget file in Mpc/h.  */
          else if (strcmp (long_options[option_index].name, "gadgetUnit") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->gadgetUnit_arg), 
                 &(args_info->gadgetUnit_orig), &(args_info->gadgetUnit_given),
                &(local_args_info.gadgetUnit_given), optarg, 0, "0.001", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "gadgetUnit", '-',
                additional_error))
              goto failure;
          
          }
          /* Seed for random number generation to select the subsample.  */
          else if (strcmp (long_options[option_index].name, "subsample_seed") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->subsample_seed_arg), 
                 &(args_info->subsample_seed_orig), &(args_info->subsample_seed_given),
                &(local_args_info.subsample_seed_given), optarg, 0, "190524", ARG_INT,
                check_ambiguity, override, 0, 0,
                "subsample_seed", '-',
                additional_error))
              goto failure;
          
          }
          /* Resubsampling factor compared to the subsampled simulation.  */
          else if (strcmp (long_options[option_index].name, "resubsample") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->resubsample_arg), 
                 &(args_info->resubsample_orig), &(args_info->resubsample_given),
                &(local_args_info.resubsample_given), optarg, 0, 0, ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "resubsample", '-',
                additional_error))
              goto failure;
          
          }
          /* Seed for resubsampling from a subsampled simulation.  */
          else if (strcmp (long_options[option_index].name, "resubsample_seed") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->resubsample_seed_arg), 
                 &(args_info->resubsample_seed_orig), &(args_info->resubsample_seed_given),
                &(local_args_info.resubsample_seed_given), optarg, 0, "20132011", ARG_INT,
                check_ambiguity, override, 0, 0,
                "resubsample_seed", '-',
                additional_error))
              goto failure;
          
          }
          /* Slightly joggle the input particle positions.  */
          else if (strcmp (long_options[option_index].name, "joggleParticles") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->joggleParticles_flag), 0, &(args_info->joggleParticles_given),
                &(local_args_info.joggleParticles_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "joggleParticles", '-',
                additional_error))
              goto failure;
          
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", GENERATEMOCK_CONF_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += generateMock_conf_required2 (args_info, argv[0], additional_error);
    }

  generateMock_conf_release (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  generateMock_conf_release (&local_args_info);
  return (EXIT_FAILURE);
}

#ifndef CONFIG_FILE_LINE_SIZE
#define CONFIG_FILE_LINE_SIZE 2048
#endif
#define ADDITIONAL_ERROR " in configuration file "

#define CONFIG_FILE_LINE_BUFFER_SIZE (CONFIG_FILE_LINE_SIZE+3)
/* 3 is for "--" and "=" */

static int
_generateMock_conf_configfile (const char *filename, int *my_argc)
{
  FILE* file;
  char my_argv[CONFIG_FILE_LINE_BUFFER_SIZE+1];
  char linebuf[CONFIG_FILE_LINE_SIZE];
  int line_num = 0;
  int result = 0, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;

  if ((file = fopen(filename, "r")) == 0)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               GENERATEMOCK_CONF_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  while ((fgets(linebuf, CONFIG_FILE_LINE_SIZE, file)) != 0)
    {
      ++line_num;
      my_argv[0] = '\0';
      len = strlen(linebuf);
      if (len > (CONFIG_FILE_LINE_BUFFER_SIZE-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   GENERATEMOCK_CONF_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          break;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn (linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = 0;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");

      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 GENERATEMOCK_CONF_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 GENERATEMOCK_CONF_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }

    noarg:
      if (!strcmp(fopt,"include")) {
        if (farg && *farg) {
          result = _generateMock_conf_configfile(farg, my_argc);
        } else {
          fprintf(stderr, "%s:%s:%d: include requires a filename argument.\n",
                  GENERATEMOCK_CONF_PACKAGE, filename, line_num);
        }
        continue;
      }
      len = strlen(fopt);
      strcat (my_argv, len > 1 ? "--" : "-");
      strcat (my_argv, fopt);
      if (len > 1 && ((farg && *farg) || equal))
        strcat (my_argv, "=");
      if (farg && *farg)
        strcat (my_argv, farg);
      ++(*my_argc);

      cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
      cmd_line_list_tmp->next = cmd_line_list;
      cmd_line_list = cmd_line_list_tmp;
      cmd_line_list->string_arg = gengetopt_strdup(my_argv);
    } /* while */

  if (file)
    fclose(file);
  return result;
}

int
generateMock_conf_configfile (
  const char *filename,
                           struct generateMock_info *args_info,
                           int override, int initialize, int check_required)
{
  struct generateMock_conf_params params;

  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;
  
  return generateMock_conf_config_file (filename, args_info, &params);
}

int
generateMock_conf_config_file (const char *filename,
                           struct generateMock_info *args_info,
                           struct generateMock_conf_params *params)
{
  int i, result;
  int my_argc = 1;
  char **my_argv_arg;
  char *additional_error;

  /* store the program name */
  cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
  cmd_line_list_tmp->next = cmd_line_list;
  cmd_line_list = cmd_line_list_tmp;
  cmd_line_list->string_arg = gengetopt_strdup (GENERATEMOCK_CONF_PACKAGE);

  result = _generateMock_conf_configfile(filename, &my_argc);

  if (result != EXIT_FAILURE) {
    my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
    cmd_line_list_tmp = cmd_line_list;

    for (i = my_argc - 1; i >= 0; --i) {
      my_argv_arg[i] = cmd_line_list_tmp->string_arg;
      cmd_line_list_tmp = cmd_line_list_tmp->next;
    }

    my_argv_arg[my_argc] = 0;

    additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
    strcpy (additional_error, ADDITIONAL_ERROR);
    strcat (additional_error, filename);
    result =
      generateMock_conf_internal (my_argc, my_argv_arg, args_info,
                              params,
                              additional_error);

    free (additional_error);
    free (my_argv_arg);
  }

  free_cmd_list();
  if (result == EXIT_FAILURE)
    {
      generateMock_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}
