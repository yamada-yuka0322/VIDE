/*
  File autogenerated by gengetopt version 2.22.5
  generated with the following command:
  ../../ext_install/bin/gengetopt -i /mnt/data/yuka/repository/vide_public/c_tools/stacking/pruneVoids.ggo -f pruneVoids_conf -a pruneVoids_info -F pruneVoids_conf -C zobovConf_info 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef FIX_UNUSED
#define FIX_UNUSED(X) (void) (X) /* avoid warnings for unused params */
#endif

#include <getopt.h>

#include "pruneVoids_conf.h"

const char *pruneVoids_info_purpose = "";

const char *pruneVoids_info_usage = "Usage: removeEdgeVoids [OPTIONS]...";

const char *pruneVoids_info_description = "";

const char *pruneVoids_info_help[] = {
  "  -h, --help                   Print help and exit",
  "  -V, --version                Print version and exit",
  "      --configFile=STRING      Configuration filename",
  "      --partFile=STRING        Particle file from generateFromCatalog",
  "      --extraInfo=STRING       Extra particle file from generateFromCatalog",
  "      --voidDesc=STRING        Void list from ZOBOV",
  "      --void2Zone=STRING       Zone file from ZOBOV",
  "      --partVol=STRING         Particle volume file from ZOBOV",
  "      --partAdj=STRING         Adjacency file from ZOBOV",
  "      --zone2Part=STRING       Particle file from ZOBOV",
  "      --mockIndex=INT          Beginning index of mock particles",
  "      --numVoids=INT           Number of voids",
  "      --isObservation          We are working with observational data  \n                                 (default=off)",
  "      --useComoving            Void positions are in comoving coordinates  \n                                 (default=off)",
  "      --omegaM=DOUBLE          Omega_M for redshift convertion  \n                                 (default=`0.27')",
  "      --zMin=DOUBLE            Minimum redshift of sample  (default=`0.0')",
  "      --zMax=DOUBLE            Maximum redshift of sample  (default=`10.0')",
  "      --rMin=DOUBLE            Minimum allowable void radius  (default=`0.0')",
  "      --outputDir=STRING       Directory to place outputs",
  "      --sampleName=STRING      unique string to assign to outputs",
  "      --periodic=STRING        Set of edges which are periodic  (default=`xy')",
  "      --tolerance=DOUBLE       Fraction of void width to consider edge  \n                                 (default=`1.0')",
  "      --centralRadFrac=DOUBLE  Fraction of void radii to consider central \n                                 region  (default=`4')",
  "      --maxCentralDen=DOUBLE   Maximum central density to accept as a void  \n                                 (default=`0.2')",
    0
};

typedef enum {ARG_NO
  , ARG_FLAG
  , ARG_STRING
  , ARG_INT
  , ARG_DOUBLE
} pruneVoids_conf_arg_type;

static
void clear_given (struct pruneVoids_info *args_info);
static
void clear_args (struct pruneVoids_info *args_info);

static int
pruneVoids_conf_internal (int argc, char **argv, struct pruneVoids_info *args_info,
                        struct pruneVoids_conf_params *params, const char *additional_error);

static int
pruneVoids_conf_required2 (struct pruneVoids_info *args_info, const char *prog_name, const char *additional_error);
struct line_list
{
  char * string_arg;
  struct line_list * next;
};

static struct line_list *cmd_line_list = 0;
static struct line_list *cmd_line_list_tmp = 0;

static void
free_cmd_list(void)
{
  /* free the list of a previous call */
  if (cmd_line_list)
    {
      while (cmd_line_list) {
        cmd_line_list_tmp = cmd_line_list;
        cmd_line_list = cmd_line_list->next;
        free (cmd_line_list_tmp->string_arg);
        free (cmd_line_list_tmp);
      }
    }
}


static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct pruneVoids_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->configFile_given = 0 ;
  args_info->partFile_given = 0 ;
  args_info->extraInfo_given = 0 ;
  args_info->voidDesc_given = 0 ;
  args_info->void2Zone_given = 0 ;
  args_info->partVol_given = 0 ;
  args_info->partAdj_given = 0 ;
  args_info->zone2Part_given = 0 ;
  args_info->mockIndex_given = 0 ;
  args_info->numVoids_given = 0 ;
  args_info->isObservation_given = 0 ;
  args_info->useComoving_given = 0 ;
  args_info->omegaM_given = 0 ;
  args_info->zMin_given = 0 ;
  args_info->zMax_given = 0 ;
  args_info->rMin_given = 0 ;
  args_info->outputDir_given = 0 ;
  args_info->sampleName_given = 0 ;
  args_info->periodic_given = 0 ;
  args_info->tolerance_given = 0 ;
  args_info->centralRadFrac_given = 0 ;
  args_info->maxCentralDen_given = 0 ;
}

static
void clear_args (struct pruneVoids_info *args_info)
{
  FIX_UNUSED (args_info);
  args_info->configFile_arg = NULL;
  args_info->configFile_orig = NULL;
  args_info->partFile_arg = NULL;
  args_info->partFile_orig = NULL;
  args_info->extraInfo_arg = NULL;
  args_info->extraInfo_orig = NULL;
  args_info->voidDesc_arg = NULL;
  args_info->voidDesc_orig = NULL;
  args_info->void2Zone_arg = NULL;
  args_info->void2Zone_orig = NULL;
  args_info->partVol_arg = NULL;
  args_info->partVol_orig = NULL;
  args_info->partAdj_arg = NULL;
  args_info->partAdj_orig = NULL;
  args_info->zone2Part_arg = NULL;
  args_info->zone2Part_orig = NULL;
  args_info->mockIndex_orig = NULL;
  args_info->numVoids_orig = NULL;
  args_info->isObservation_flag = 0;
  args_info->useComoving_flag = 0;
  args_info->omegaM_arg = 0.27;
  args_info->omegaM_orig = NULL;
  args_info->zMin_arg = 0.0;
  args_info->zMin_orig = NULL;
  args_info->zMax_arg = 10.0;
  args_info->zMax_orig = NULL;
  args_info->rMin_arg = 0.0;
  args_info->rMin_orig = NULL;
  args_info->outputDir_arg = NULL;
  args_info->outputDir_orig = NULL;
  args_info->sampleName_arg = NULL;
  args_info->sampleName_orig = NULL;
  args_info->periodic_arg = gengetopt_strdup ("xy");
  args_info->periodic_orig = NULL;
  args_info->tolerance_arg = 1.0;
  args_info->tolerance_orig = NULL;
  args_info->centralRadFrac_arg = 4;
  args_info->centralRadFrac_orig = NULL;
  args_info->maxCentralDen_arg = 0.2;
  args_info->maxCentralDen_orig = NULL;
  
}

static
void init_args_info(struct pruneVoids_info *args_info)
{


  args_info->help_help = pruneVoids_info_help[0] ;
  args_info->version_help = pruneVoids_info_help[1] ;
  args_info->configFile_help = pruneVoids_info_help[2] ;
  args_info->partFile_help = pruneVoids_info_help[3] ;
  args_info->extraInfo_help = pruneVoids_info_help[4] ;
  args_info->voidDesc_help = pruneVoids_info_help[5] ;
  args_info->void2Zone_help = pruneVoids_info_help[6] ;
  args_info->partVol_help = pruneVoids_info_help[7] ;
  args_info->partAdj_help = pruneVoids_info_help[8] ;
  args_info->zone2Part_help = pruneVoids_info_help[9] ;
  args_info->mockIndex_help = pruneVoids_info_help[10] ;
  args_info->numVoids_help = pruneVoids_info_help[11] ;
  args_info->isObservation_help = pruneVoids_info_help[12] ;
  args_info->useComoving_help = pruneVoids_info_help[13] ;
  args_info->omegaM_help = pruneVoids_info_help[14] ;
  args_info->zMin_help = pruneVoids_info_help[15] ;
  args_info->zMax_help = pruneVoids_info_help[16] ;
  args_info->rMin_help = pruneVoids_info_help[17] ;
  args_info->outputDir_help = pruneVoids_info_help[18] ;
  args_info->sampleName_help = pruneVoids_info_help[19] ;
  args_info->periodic_help = pruneVoids_info_help[20] ;
  args_info->tolerance_help = pruneVoids_info_help[21] ;
  args_info->centralRadFrac_help = pruneVoids_info_help[22] ;
  args_info->maxCentralDen_help = pruneVoids_info_help[23] ;
  
}

void
pruneVoids_conf_print_version (void)
{
  printf ("%s %s\n",
     (strlen(PRUNEVOIDS_CONF_PACKAGE_NAME) ? PRUNEVOIDS_CONF_PACKAGE_NAME : PRUNEVOIDS_CONF_PACKAGE),
     PRUNEVOIDS_CONF_VERSION);
}

static void print_help_common(void) {
  pruneVoids_conf_print_version ();

  if (strlen(pruneVoids_info_purpose) > 0)
    printf("\n%s\n", pruneVoids_info_purpose);

  if (strlen(pruneVoids_info_usage) > 0)
    printf("\n%s\n", pruneVoids_info_usage);

  printf("\n");

  if (strlen(pruneVoids_info_description) > 0)
    printf("%s\n\n", pruneVoids_info_description);
}

void
pruneVoids_conf_print_help (void)
{
  int i = 0;
  print_help_common();
  while (pruneVoids_info_help[i])
    printf("%s\n", pruneVoids_info_help[i++]);
}

void
pruneVoids_conf_init (struct pruneVoids_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);
}

void
pruneVoids_conf_params_init(struct pruneVoids_conf_params *params)
{
  if (params)
    { 
      params->override = 0;
      params->initialize = 1;
      params->check_required = 1;
      params->check_ambiguity = 0;
      params->print_errors = 1;
    }
}

struct pruneVoids_conf_params *
pruneVoids_conf_params_create(void)
{
  struct pruneVoids_conf_params *params = 
    (struct pruneVoids_conf_params *)malloc(sizeof(struct pruneVoids_conf_params));
  pruneVoids_conf_params_init(params);  
  return params;
}

static void
free_string_field (char **s)
{
  if (*s)
    {
      free (*s);
      *s = 0;
    }
}


static void
pruneVoids_conf_release (struct pruneVoids_info *args_info)
{

  free_string_field (&(args_info->configFile_arg));
  free_string_field (&(args_info->configFile_orig));
  free_string_field (&(args_info->partFile_arg));
  free_string_field (&(args_info->partFile_orig));
  free_string_field (&(args_info->extraInfo_arg));
  free_string_field (&(args_info->extraInfo_orig));
  free_string_field (&(args_info->voidDesc_arg));
  free_string_field (&(args_info->voidDesc_orig));
  free_string_field (&(args_info->void2Zone_arg));
  free_string_field (&(args_info->void2Zone_orig));
  free_string_field (&(args_info->partVol_arg));
  free_string_field (&(args_info->partVol_orig));
  free_string_field (&(args_info->partAdj_arg));
  free_string_field (&(args_info->partAdj_orig));
  free_string_field (&(args_info->zone2Part_arg));
  free_string_field (&(args_info->zone2Part_orig));
  free_string_field (&(args_info->mockIndex_orig));
  free_string_field (&(args_info->numVoids_orig));
  free_string_field (&(args_info->omegaM_orig));
  free_string_field (&(args_info->zMin_orig));
  free_string_field (&(args_info->zMax_orig));
  free_string_field (&(args_info->rMin_orig));
  free_string_field (&(args_info->outputDir_arg));
  free_string_field (&(args_info->outputDir_orig));
  free_string_field (&(args_info->sampleName_arg));
  free_string_field (&(args_info->sampleName_orig));
  free_string_field (&(args_info->periodic_arg));
  free_string_field (&(args_info->periodic_orig));
  free_string_field (&(args_info->tolerance_orig));
  free_string_field (&(args_info->centralRadFrac_orig));
  free_string_field (&(args_info->maxCentralDen_orig));
  
  

  clear_given (args_info);
}


static void
write_into_file(FILE *outfile, const char *opt, const char *arg, const char *values[])
{
  FIX_UNUSED (values);
  if (arg) {
    fprintf(outfile, "%s=\"%s\"\n", opt, arg);
  } else {
    fprintf(outfile, "%s\n", opt);
  }
}


int
pruneVoids_conf_dump(FILE *outfile, struct pruneVoids_info *args_info)
{
  int i = 0;

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot dump options to stream\n", PRUNEVOIDS_CONF_PACKAGE);
      return EXIT_FAILURE;
    }

  if (args_info->help_given)
    write_into_file(outfile, "help", 0, 0 );
  if (args_info->version_given)
    write_into_file(outfile, "version", 0, 0 );
  if (args_info->configFile_given)
    write_into_file(outfile, "configFile", args_info->configFile_orig, 0);
  if (args_info->partFile_given)
    write_into_file(outfile, "partFile", args_info->partFile_orig, 0);
  if (args_info->extraInfo_given)
    write_into_file(outfile, "extraInfo", args_info->extraInfo_orig, 0);
  if (args_info->voidDesc_given)
    write_into_file(outfile, "voidDesc", args_info->voidDesc_orig, 0);
  if (args_info->void2Zone_given)
    write_into_file(outfile, "void2Zone", args_info->void2Zone_orig, 0);
  if (args_info->partVol_given)
    write_into_file(outfile, "partVol", args_info->partVol_orig, 0);
  if (args_info->partAdj_given)
    write_into_file(outfile, "partAdj", args_info->partAdj_orig, 0);
  if (args_info->zone2Part_given)
    write_into_file(outfile, "zone2Part", args_info->zone2Part_orig, 0);
  if (args_info->mockIndex_given)
    write_into_file(outfile, "mockIndex", args_info->mockIndex_orig, 0);
  if (args_info->numVoids_given)
    write_into_file(outfile, "numVoids", args_info->numVoids_orig, 0);
  if (args_info->isObservation_given)
    write_into_file(outfile, "isObservation", 0, 0 );
  if (args_info->useComoving_given)
    write_into_file(outfile, "useComoving", 0, 0 );
  if (args_info->omegaM_given)
    write_into_file(outfile, "omegaM", args_info->omegaM_orig, 0);
  if (args_info->zMin_given)
    write_into_file(outfile, "zMin", args_info->zMin_orig, 0);
  if (args_info->zMax_given)
    write_into_file(outfile, "zMax", args_info->zMax_orig, 0);
  if (args_info->rMin_given)
    write_into_file(outfile, "rMin", args_info->rMin_orig, 0);
  if (args_info->outputDir_given)
    write_into_file(outfile, "outputDir", args_info->outputDir_orig, 0);
  if (args_info->sampleName_given)
    write_into_file(outfile, "sampleName", args_info->sampleName_orig, 0);
  if (args_info->periodic_given)
    write_into_file(outfile, "periodic", args_info->periodic_orig, 0);
  if (args_info->tolerance_given)
    write_into_file(outfile, "tolerance", args_info->tolerance_orig, 0);
  if (args_info->centralRadFrac_given)
    write_into_file(outfile, "centralRadFrac", args_info->centralRadFrac_orig, 0);
  if (args_info->maxCentralDen_given)
    write_into_file(outfile, "maxCentralDen", args_info->maxCentralDen_orig, 0);
  

  i = EXIT_SUCCESS;
  return i;
}

int
pruneVoids_conf_file_save(const char *filename, struct pruneVoids_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", PRUNEVOIDS_CONF_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  i = pruneVoids_conf_dump(outfile, args_info);
  fclose (outfile);

  return i;
}

void
pruneVoids_conf_free (struct pruneVoids_info *args_info)
{
  pruneVoids_conf_release (args_info);
}

/** @brief replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = 0;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
pruneVoids_conf (int argc, char **argv, struct pruneVoids_info *args_info)
{
  return pruneVoids_conf2 (argc, argv, args_info, 0, 1, 1);
}

int
pruneVoids_conf_ext (int argc, char **argv, struct pruneVoids_info *args_info,
                   struct pruneVoids_conf_params *params)
{
  int result;
  result = pruneVoids_conf_internal (argc, argv, args_info, params, 0);

  if (result == EXIT_FAILURE)
    {
      pruneVoids_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
pruneVoids_conf2 (int argc, char **argv, struct pruneVoids_info *args_info, int override, int initialize, int check_required)
{
  int result;
  struct pruneVoids_conf_params params;
  
  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;

  result = pruneVoids_conf_internal (argc, argv, args_info, &params, 0);

  if (result == EXIT_FAILURE)
    {
      pruneVoids_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
pruneVoids_conf_required (struct pruneVoids_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (pruneVoids_conf_required2(args_info, prog_name, 0) > 0)
    result = EXIT_FAILURE;

  if (result == EXIT_FAILURE)
    {
      pruneVoids_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
pruneVoids_conf_required2 (struct pruneVoids_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;
  FIX_UNUSED (additional_error);

  /* checks for required options */
  if (! args_info->partFile_given)
    {
      fprintf (stderr, "%s: '--partFile' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->extraInfo_given)
    {
      fprintf (stderr, "%s: '--extraInfo' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->voidDesc_given)
    {
      fprintf (stderr, "%s: '--voidDesc' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->void2Zone_given)
    {
      fprintf (stderr, "%s: '--void2Zone' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->partVol_given)
    {
      fprintf (stderr, "%s: '--partVol' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->partAdj_given)
    {
      fprintf (stderr, "%s: '--partAdj' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->zone2Part_given)
    {
      fprintf (stderr, "%s: '--zone2Part' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->mockIndex_given)
    {
      fprintf (stderr, "%s: '--mockIndex' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->numVoids_given)
    {
      fprintf (stderr, "%s: '--numVoids' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->outputDir_given)
    {
      fprintf (stderr, "%s: '--outputDir' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->sampleName_given)
    {
      fprintf (stderr, "%s: '--sampleName' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  
  /* checks for dependences among options */

  return error;
}


static char *package_name = 0;

/**
 * @brief updates an option
 * @param field the generic pointer to the field to update
 * @param orig_field the pointer to the orig field
 * @param field_given the pointer to the number of occurrence of this option
 * @param prev_given the pointer to the number of occurrence already seen
 * @param value the argument for this option (if null no arg was specified)
 * @param possible_values the possible values for this option (if specified)
 * @param default_value the default value (in case the option only accepts fixed values)
 * @param arg_type the type of this option
 * @param check_ambiguity @see pruneVoids_conf_params.check_ambiguity
 * @param override @see pruneVoids_conf_params.override
 * @param no_free whether to free a possible previous value
 * @param multiple_option whether this is a multiple option
 * @param long_opt the corresponding long option
 * @param short_opt the corresponding short option (or '-' if none)
 * @param additional_error possible further error specification
 */
static
int update_arg(void *field, char **orig_field,
               unsigned int *field_given, unsigned int *prev_given, 
               char *value, const char *possible_values[],
               const char *default_value,
               pruneVoids_conf_arg_type arg_type,
               int check_ambiguity, int override,
               int no_free, int multiple_option,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  char *stop_char = 0;
  const char *val = value;
  int found;
  char **string_field;
  FIX_UNUSED (field);

  stop_char = 0;
  found = 0;

  if (!multiple_option && prev_given && (*prev_given || (check_ambiguity && *field_given)))
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: `--%s' (`-%c') option given more than once%s\n", 
               package_name, long_opt, short_opt,
               (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: `--%s' option given more than once%s\n", 
               package_name, long_opt,
               (additional_error ? additional_error : ""));
      return 1; /* failure */
    }

  FIX_UNUSED (default_value);
    
  if (field_given && *field_given && ! override)
    return 0;
  if (prev_given)
    (*prev_given)++;
  if (field_given)
    (*field_given)++;
  if (possible_values)
    val = possible_values[found];

  switch(arg_type) {
  case ARG_FLAG:
    *((int *)field) = !*((int *)field);
    break;
  case ARG_INT:
    if (val) *((int *)field) = strtol (val, &stop_char, 0);
    break;
  case ARG_DOUBLE:
    if (val) *((double *)field) = strtod (val, &stop_char);
    break;
  case ARG_STRING:
    if (val) {
      string_field = (char **)field;
      if (!no_free && *string_field)
        free (*string_field); /* free previous string */
      *string_field = gengetopt_strdup (val);
    }
    break;
  default:
    break;
  };

  /* check numeric conversion */
  switch(arg_type) {
  case ARG_INT:
  case ARG_DOUBLE:
    if (val && !(stop_char && *stop_char == '\0')) {
      fprintf(stderr, "%s: invalid numeric value: %s\n", package_name, val);
      return 1; /* failure */
    }
    break;
  default:
    ;
  };

  /* store the original value */
  switch(arg_type) {
  case ARG_NO:
  case ARG_FLAG:
    break;
  default:
    if (value && orig_field) {
      if (no_free) {
        *orig_field = value;
      } else {
        if (*orig_field)
          free (*orig_field); /* free previous string */
        *orig_field = gengetopt_strdup (value);
      }
    }
  };

  return 0; /* OK */
}


int
pruneVoids_conf_internal (
  int argc, char **argv, struct pruneVoids_info *args_info,
                        struct pruneVoids_conf_params *params, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct pruneVoids_info local_args_info;
  
  int override;
  int initialize;
  int check_required;
  int check_ambiguity;
  
  package_name = argv[0];
  
  override = params->override;
  initialize = params->initialize;
  check_required = params->check_required;
  check_ambiguity = params->check_ambiguity;

  if (initialize)
    pruneVoids_conf_init (args_info);

  pruneVoids_conf_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = params->print_errors;
  optopt = '?';

  while (1)
    {
      int option_index = 0;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "configFile",	1, NULL, 0 },
        { "partFile",	1, NULL, 0 },
        { "extraInfo",	1, NULL, 0 },
        { "voidDesc",	1, NULL, 0 },
        { "void2Zone",	1, NULL, 0 },
        { "partVol",	1, NULL, 0 },
        { "partAdj",	1, NULL, 0 },
        { "zone2Part",	1, NULL, 0 },
        { "mockIndex",	1, NULL, 0 },
        { "numVoids",	1, NULL, 0 },
        { "isObservation",	0, NULL, 0 },
        { "useComoving",	0, NULL, 0 },
        { "omegaM",	1, NULL, 0 },
        { "zMin",	1, NULL, 0 },
        { "zMax",	1, NULL, 0 },
        { "rMin",	1, NULL, 0 },
        { "outputDir",	1, NULL, 0 },
        { "sampleName",	1, NULL, 0 },
        { "periodic",	1, NULL, 0 },
        { "tolerance",	1, NULL, 0 },
        { "centralRadFrac",	1, NULL, 0 },
        { "maxCentralDen",	1, NULL, 0 },
        { 0,  0, 0, 0 }
      };

      c = getopt_long (argc, argv, "hV", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          pruneVoids_conf_print_help ();
          pruneVoids_conf_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          pruneVoids_conf_print_version ();
          pruneVoids_conf_free (&local_args_info);
          exit (EXIT_SUCCESS);


        case 0:	/* Long option with no short option */
          /* Configuration filename.  */
          if (strcmp (long_options[option_index].name, "configFile") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->configFile_arg), 
                 &(args_info->configFile_orig), &(args_info->configFile_given),
                &(local_args_info.configFile_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "configFile", '-',
                additional_error))
              goto failure;
          
          }
          /* Particle file from generateFromCatalog.  */
          else if (strcmp (long_options[option_index].name, "partFile") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->partFile_arg), 
                 &(args_info->partFile_orig), &(args_info->partFile_given),
                &(local_args_info.partFile_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "partFile", '-',
                additional_error))
              goto failure;
          
          }
          /* Extra particle file from generateFromCatalog.  */
          else if (strcmp (long_options[option_index].name, "extraInfo") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->extraInfo_arg), 
                 &(args_info->extraInfo_orig), &(args_info->extraInfo_given),
                &(local_args_info.extraInfo_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "extraInfo", '-',
                additional_error))
              goto failure;
          
          }
          /* Void list from ZOBOV.  */
          else if (strcmp (long_options[option_index].name, "voidDesc") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->voidDesc_arg), 
                 &(args_info->voidDesc_orig), &(args_info->voidDesc_given),
                &(local_args_info.voidDesc_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "voidDesc", '-',
                additional_error))
              goto failure;
          
          }
          /* Zone file from ZOBOV.  */
          else if (strcmp (long_options[option_index].name, "void2Zone") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->void2Zone_arg), 
                 &(args_info->void2Zone_orig), &(args_info->void2Zone_given),
                &(local_args_info.void2Zone_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "void2Zone", '-',
                additional_error))
              goto failure;
          
          }
          /* Particle volume file from ZOBOV.  */
          else if (strcmp (long_options[option_index].name, "partVol") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->partVol_arg), 
                 &(args_info->partVol_orig), &(args_info->partVol_given),
                &(local_args_info.partVol_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "partVol", '-',
                additional_error))
              goto failure;
          
          }
          /* Adjacency file from ZOBOV.  */
          else if (strcmp (long_options[option_index].name, "partAdj") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->partAdj_arg), 
                 &(args_info->partAdj_orig), &(args_info->partAdj_given),
                &(local_args_info.partAdj_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "partAdj", '-',
                additional_error))
              goto failure;
          
          }
          /* Particle file from ZOBOV.  */
          else if (strcmp (long_options[option_index].name, "zone2Part") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->zone2Part_arg), 
                 &(args_info->zone2Part_orig), &(args_info->zone2Part_given),
                &(local_args_info.zone2Part_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "zone2Part", '-',
                additional_error))
              goto failure;
          
          }
          /* Beginning index of mock particles.  */
          else if (strcmp (long_options[option_index].name, "mockIndex") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->mockIndex_arg), 
                 &(args_info->mockIndex_orig), &(args_info->mockIndex_given),
                &(local_args_info.mockIndex_given), optarg, 0, 0, ARG_INT,
                check_ambiguity, override, 0, 0,
                "mockIndex", '-',
                additional_error))
              goto failure;
          
          }
          /* Number of voids.  */
          else if (strcmp (long_options[option_index].name, "numVoids") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->numVoids_arg), 
                 &(args_info->numVoids_orig), &(args_info->numVoids_given),
                &(local_args_info.numVoids_given), optarg, 0, 0, ARG_INT,
                check_ambiguity, override, 0, 0,
                "numVoids", '-',
                additional_error))
              goto failure;
          
          }
          /* We are working with observational data.  */
          else if (strcmp (long_options[option_index].name, "isObservation") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->isObservation_flag), 0, &(args_info->isObservation_given),
                &(local_args_info.isObservation_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "isObservation", '-',
                additional_error))
              goto failure;
          
          }
          /* Void positions are in comoving coordinates.  */
          else if (strcmp (long_options[option_index].name, "useComoving") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->useComoving_flag), 0, &(args_info->useComoving_given),
                &(local_args_info.useComoving_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "useComoving", '-',
                additional_error))
              goto failure;
          
          }
          /* Omega_M for redshift convertion.  */
          else if (strcmp (long_options[option_index].name, "omegaM") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->omegaM_arg), 
                 &(args_info->omegaM_orig), &(args_info->omegaM_given),
                &(local_args_info.omegaM_given), optarg, 0, "0.27", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "omegaM", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum redshift of sample.  */
          else if (strcmp (long_options[option_index].name, "zMin") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->zMin_arg), 
                 &(args_info->zMin_orig), &(args_info->zMin_given),
                &(local_args_info.zMin_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "zMin", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum redshift of sample.  */
          else if (strcmp (long_options[option_index].name, "zMax") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->zMax_arg), 
                 &(args_info->zMax_orig), &(args_info->zMax_given),
                &(local_args_info.zMax_given), optarg, 0, "10.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "zMax", '-',
                additional_error))
              goto failure;
          
          }
          /* Minimum allowable void radius.  */
          else if (strcmp (long_options[option_index].name, "rMin") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->rMin_arg), 
                 &(args_info->rMin_orig), &(args_info->rMin_given),
                &(local_args_info.rMin_given), optarg, 0, "0.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "rMin", '-',
                additional_error))
              goto failure;
          
          }
          /* Directory to place outputs.  */
          else if (strcmp (long_options[option_index].name, "outputDir") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->outputDir_arg), 
                 &(args_info->outputDir_orig), &(args_info->outputDir_given),
                &(local_args_info.outputDir_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "outputDir", '-',
                additional_error))
              goto failure;
          
          }
          /* unique string to assign to outputs.  */
          else if (strcmp (long_options[option_index].name, "sampleName") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->sampleName_arg), 
                 &(args_info->sampleName_orig), &(args_info->sampleName_given),
                &(local_args_info.sampleName_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "sampleName", '-',
                additional_error))
              goto failure;
          
          }
          /* Set of edges which are periodic.  */
          else if (strcmp (long_options[option_index].name, "periodic") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->periodic_arg), 
                 &(args_info->periodic_orig), &(args_info->periodic_given),
                &(local_args_info.periodic_given), optarg, 0, "xy", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "periodic", '-',
                additional_error))
              goto failure;
          
          }
          /* Fraction of void width to consider edge.  */
          else if (strcmp (long_options[option_index].name, "tolerance") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->tolerance_arg), 
                 &(args_info->tolerance_orig), &(args_info->tolerance_given),
                &(local_args_info.tolerance_given), optarg, 0, "1.0", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "tolerance", '-',
                additional_error))
              goto failure;
          
          }
          /* Fraction of void radii to consider central region.  */
          else if (strcmp (long_options[option_index].name, "centralRadFrac") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->centralRadFrac_arg), 
                 &(args_info->centralRadFrac_orig), &(args_info->centralRadFrac_given),
                &(local_args_info.centralRadFrac_given), optarg, 0, "4", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "centralRadFrac", '-',
                additional_error))
              goto failure;
          
          }
          /* Maximum central density to accept as a void.  */
          else if (strcmp (long_options[option_index].name, "maxCentralDen") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->maxCentralDen_arg), 
                 &(args_info->maxCentralDen_orig), &(args_info->maxCentralDen_given),
                &(local_args_info.maxCentralDen_given), optarg, 0, "0.2", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "maxCentralDen", '-',
                additional_error))
              goto failure;
          
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", PRUNEVOIDS_CONF_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += pruneVoids_conf_required2 (args_info, argv[0], additional_error);
    }

  pruneVoids_conf_release (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  pruneVoids_conf_release (&local_args_info);
  return (EXIT_FAILURE);
}

#ifndef CONFIG_FILE_LINE_SIZE
#define CONFIG_FILE_LINE_SIZE 2048
#endif
#define ADDITIONAL_ERROR " in configuration file "

#define CONFIG_FILE_LINE_BUFFER_SIZE (CONFIG_FILE_LINE_SIZE+3)
/* 3 is for "--" and "=" */

static int
_pruneVoids_conf_configfile (const char *filename, int *my_argc)
{
  FILE* file;
  char my_argv[CONFIG_FILE_LINE_BUFFER_SIZE+1];
  char linebuf[CONFIG_FILE_LINE_SIZE];
  int line_num = 0;
  int result = 0, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;

  if ((file = fopen(filename, "r")) == 0)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               PRUNEVOIDS_CONF_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  while ((fgets(linebuf, CONFIG_FILE_LINE_SIZE, file)) != 0)
    {
      ++line_num;
      my_argv[0] = '\0';
      len = strlen(linebuf);
      if (len > (CONFIG_FILE_LINE_BUFFER_SIZE-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   PRUNEVOIDS_CONF_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          break;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn (linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = 0;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");

      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 PRUNEVOIDS_CONF_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 PRUNEVOIDS_CONF_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }

    noarg:
      if (!strcmp(fopt,"include")) {
        if (farg && *farg) {
          result = _pruneVoids_conf_configfile(farg, my_argc);
        } else {
          fprintf(stderr, "%s:%s:%d: include requires a filename argument.\n",
                  PRUNEVOIDS_CONF_PACKAGE, filename, line_num);
        }
        continue;
      }
      len = strlen(fopt);
      strcat (my_argv, len > 1 ? "--" : "-");
      strcat (my_argv, fopt);
      if (len > 1 && ((farg && *farg) || equal))
        strcat (my_argv, "=");
      if (farg && *farg)
        strcat (my_argv, farg);
      ++(*my_argc);

      cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
      cmd_line_list_tmp->next = cmd_line_list;
      cmd_line_list = cmd_line_list_tmp;
      cmd_line_list->string_arg = gengetopt_strdup(my_argv);
    } /* while */

  if (file)
    fclose(file);
  return result;
}

int
pruneVoids_conf_configfile (
  const char *filename,
                           struct pruneVoids_info *args_info,
                           int override, int initialize, int check_required)
{
  struct pruneVoids_conf_params params;

  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;
  
  return pruneVoids_conf_config_file (filename, args_info, &params);
}

int
pruneVoids_conf_config_file (const char *filename,
                           struct pruneVoids_info *args_info,
                           struct pruneVoids_conf_params *params)
{
  int i, result;
  int my_argc = 1;
  char **my_argv_arg;
  char *additional_error;

  /* store the program name */
  cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
  cmd_line_list_tmp->next = cmd_line_list;
  cmd_line_list = cmd_line_list_tmp;
  cmd_line_list->string_arg = gengetopt_strdup (PRUNEVOIDS_CONF_PACKAGE);

  result = _pruneVoids_conf_configfile(filename, &my_argc);

  if (result != EXIT_FAILURE) {
    my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
    cmd_line_list_tmp = cmd_line_list;

    for (i = my_argc - 1; i >= 0; --i) {
      my_argv_arg[i] = cmd_line_list_tmp->string_arg;
      cmd_line_list_tmp = cmd_line_list_tmp->next;
    }

    my_argv_arg[my_argc] = 0;

    additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
    strcpy (additional_error, ADDITIONAL_ERROR);
    strcat (additional_error, filename);
    result =
      pruneVoids_conf_internal (my_argc, my_argv_arg, args_info,
                              params,
                              additional_error);

    free (additional_error);
    free (my_argv_arg);
  }

  free_cmd_list();
  if (result == EXIT_FAILURE)
    {
      pruneVoids_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}
