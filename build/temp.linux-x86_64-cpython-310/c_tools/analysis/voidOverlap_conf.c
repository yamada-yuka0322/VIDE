/*
  File autogenerated by gengetopt version 2.22.5
  generated with the following command:
  ../../ext_install/bin/gengetopt -i /mnt/data/yuka/repository/vide_public/c_tools/analysis/voidOverlap.ggo -f voidOverlap_conf -a voidOverlap_info -F voidOverlap_conf -C zobovConf_info 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/

/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef FIX_UNUSED
#define FIX_UNUSED(X) (void) (X) /* avoid warnings for unused params */
#endif

#include <getopt.h>

#include "voidOverlap_conf.h"

const char *voidOverlap_info_purpose = "";

const char *voidOverlap_info_usage = "Usage: voidOverlap [OPTIONS]...";

const char *voidOverlap_info_description = "";

const char *voidOverlap_info_help[] = {
  "  -h, --help                  Print help and exit",
  "  -V, --version               Print version and exit",
  "      --configFile=STRING     Configuration file path",
  "      --partFile1=STRING      Particle file for catalog 1",
  "      --volFile1=STRING       Volume file for catalog 1",
  "      --voidFile1=STRING      Void info file for catalog 1",
  "      --infoFile1=STRING      Extra info file for catalog 1",
  "      --zoneFile1=STRING      Zone file for catalog 1",
  "      --zonePartFile1=STRING  Zone-particle file for catalog 1",
  "      --centerFile1=STRING    Barycenter file for catalog 1",
  "      --shapeFile1=STRING     Shape file for catalog 1",
  "      --partFile2=STRING      Particle file for catalog 2",
  "      --volFile2=STRING       Volume file for catalog 2",
  "      --voidFile2=STRING      Void info file for catalog 2",
  "      --infoFile2=STRING      Extra info file for catalog 2",
  "      --zoneFile2=STRING      Zone file for catalog 2",
  "      --zonePartFile2=STRING  Zone-particle file for catalog 2",
  "      --centerFile2=STRING    Barycenter file for catalog 2",
  "      --shapeFile2=STRING     Shape file for catalog 2",
  "      --isObservation         We are working with observational data  \n                                (default=off)",
  "      --outfile=STRING        Output file",
  "      --useID                 Use unique catalog ID to match voids; otherwise \n                                use volumes  (default=off)",
  "      --overlapFrac=DOUBLE    threshold fraction of voronoi radius to count as \n                                matched  (default=`0.25')",
  "      --periodic=STRING       Set of edges which are periodic  (default=`xy')",
    0
};

typedef enum {ARG_NO
  , ARG_FLAG
  , ARG_STRING
  , ARG_DOUBLE
} voidOverlap_conf_arg_type;

static
void clear_given (struct voidOverlap_info *args_info);
static
void clear_args (struct voidOverlap_info *args_info);

static int
voidOverlap_conf_internal (int argc, char **argv, struct voidOverlap_info *args_info,
                        struct voidOverlap_conf_params *params, const char *additional_error);

static int
voidOverlap_conf_required2 (struct voidOverlap_info *args_info, const char *prog_name, const char *additional_error);
struct line_list
{
  char * string_arg;
  struct line_list * next;
};

static struct line_list *cmd_line_list = 0;
static struct line_list *cmd_line_list_tmp = 0;

static void
free_cmd_list(void)
{
  /* free the list of a previous call */
  if (cmd_line_list)
    {
      while (cmd_line_list) {
        cmd_line_list_tmp = cmd_line_list;
        cmd_line_list = cmd_line_list->next;
        free (cmd_line_list_tmp->string_arg);
        free (cmd_line_list_tmp);
      }
    }
}


static char *
gengetopt_strdup (const char *s);

static
void clear_given (struct voidOverlap_info *args_info)
{
  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->configFile_given = 0 ;
  args_info->partFile1_given = 0 ;
  args_info->volFile1_given = 0 ;
  args_info->voidFile1_given = 0 ;
  args_info->infoFile1_given = 0 ;
  args_info->zoneFile1_given = 0 ;
  args_info->zonePartFile1_given = 0 ;
  args_info->centerFile1_given = 0 ;
  args_info->shapeFile1_given = 0 ;
  args_info->partFile2_given = 0 ;
  args_info->volFile2_given = 0 ;
  args_info->voidFile2_given = 0 ;
  args_info->infoFile2_given = 0 ;
  args_info->zoneFile2_given = 0 ;
  args_info->zonePartFile2_given = 0 ;
  args_info->centerFile2_given = 0 ;
  args_info->shapeFile2_given = 0 ;
  args_info->isObservation_given = 0 ;
  args_info->outfile_given = 0 ;
  args_info->useID_given = 0 ;
  args_info->overlapFrac_given = 0 ;
  args_info->periodic_given = 0 ;
}

static
void clear_args (struct voidOverlap_info *args_info)
{
  FIX_UNUSED (args_info);
  args_info->configFile_arg = NULL;
  args_info->configFile_orig = NULL;
  args_info->partFile1_arg = NULL;
  args_info->partFile1_orig = NULL;
  args_info->volFile1_arg = NULL;
  args_info->volFile1_orig = NULL;
  args_info->voidFile1_arg = NULL;
  args_info->voidFile1_orig = NULL;
  args_info->infoFile1_arg = NULL;
  args_info->infoFile1_orig = NULL;
  args_info->zoneFile1_arg = NULL;
  args_info->zoneFile1_orig = NULL;
  args_info->zonePartFile1_arg = NULL;
  args_info->zonePartFile1_orig = NULL;
  args_info->centerFile1_arg = NULL;
  args_info->centerFile1_orig = NULL;
  args_info->shapeFile1_arg = NULL;
  args_info->shapeFile1_orig = NULL;
  args_info->partFile2_arg = NULL;
  args_info->partFile2_orig = NULL;
  args_info->volFile2_arg = NULL;
  args_info->volFile2_orig = NULL;
  args_info->voidFile2_arg = NULL;
  args_info->voidFile2_orig = NULL;
  args_info->infoFile2_arg = NULL;
  args_info->infoFile2_orig = NULL;
  args_info->zoneFile2_arg = NULL;
  args_info->zoneFile2_orig = NULL;
  args_info->zonePartFile2_arg = NULL;
  args_info->zonePartFile2_orig = NULL;
  args_info->centerFile2_arg = NULL;
  args_info->centerFile2_orig = NULL;
  args_info->shapeFile2_arg = NULL;
  args_info->shapeFile2_orig = NULL;
  args_info->isObservation_flag = 0;
  args_info->outfile_arg = NULL;
  args_info->outfile_orig = NULL;
  args_info->useID_flag = 0;
  args_info->overlapFrac_arg = 0.25;
  args_info->overlapFrac_orig = NULL;
  args_info->periodic_arg = gengetopt_strdup ("xy");
  args_info->periodic_orig = NULL;
  
}

static
void init_args_info(struct voidOverlap_info *args_info)
{


  args_info->help_help = voidOverlap_info_help[0] ;
  args_info->version_help = voidOverlap_info_help[1] ;
  args_info->configFile_help = voidOverlap_info_help[2] ;
  args_info->partFile1_help = voidOverlap_info_help[3] ;
  args_info->volFile1_help = voidOverlap_info_help[4] ;
  args_info->voidFile1_help = voidOverlap_info_help[5] ;
  args_info->infoFile1_help = voidOverlap_info_help[6] ;
  args_info->zoneFile1_help = voidOverlap_info_help[7] ;
  args_info->zonePartFile1_help = voidOverlap_info_help[8] ;
  args_info->centerFile1_help = voidOverlap_info_help[9] ;
  args_info->shapeFile1_help = voidOverlap_info_help[10] ;
  args_info->partFile2_help = voidOverlap_info_help[11] ;
  args_info->volFile2_help = voidOverlap_info_help[12] ;
  args_info->voidFile2_help = voidOverlap_info_help[13] ;
  args_info->infoFile2_help = voidOverlap_info_help[14] ;
  args_info->zoneFile2_help = voidOverlap_info_help[15] ;
  args_info->zonePartFile2_help = voidOverlap_info_help[16] ;
  args_info->centerFile2_help = voidOverlap_info_help[17] ;
  args_info->shapeFile2_help = voidOverlap_info_help[18] ;
  args_info->isObservation_help = voidOverlap_info_help[19] ;
  args_info->outfile_help = voidOverlap_info_help[20] ;
  args_info->useID_help = voidOverlap_info_help[21] ;
  args_info->overlapFrac_help = voidOverlap_info_help[22] ;
  args_info->periodic_help = voidOverlap_info_help[23] ;
  
}

void
voidOverlap_conf_print_version (void)
{
  printf ("%s %s\n",
     (strlen(VOIDOVERLAP_CONF_PACKAGE_NAME) ? VOIDOVERLAP_CONF_PACKAGE_NAME : VOIDOVERLAP_CONF_PACKAGE),
     VOIDOVERLAP_CONF_VERSION);
}

static void print_help_common(void) {
  voidOverlap_conf_print_version ();

  if (strlen(voidOverlap_info_purpose) > 0)
    printf("\n%s\n", voidOverlap_info_purpose);

  if (strlen(voidOverlap_info_usage) > 0)
    printf("\n%s\n", voidOverlap_info_usage);

  printf("\n");

  if (strlen(voidOverlap_info_description) > 0)
    printf("%s\n\n", voidOverlap_info_description);
}

void
voidOverlap_conf_print_help (void)
{
  int i = 0;
  print_help_common();
  while (voidOverlap_info_help[i])
    printf("%s\n", voidOverlap_info_help[i++]);
}

void
voidOverlap_conf_init (struct voidOverlap_info *args_info)
{
  clear_given (args_info);
  clear_args (args_info);
  init_args_info (args_info);
}

void
voidOverlap_conf_params_init(struct voidOverlap_conf_params *params)
{
  if (params)
    { 
      params->override = 0;
      params->initialize = 1;
      params->check_required = 1;
      params->check_ambiguity = 0;
      params->print_errors = 1;
    }
}

struct voidOverlap_conf_params *
voidOverlap_conf_params_create(void)
{
  struct voidOverlap_conf_params *params = 
    (struct voidOverlap_conf_params *)malloc(sizeof(struct voidOverlap_conf_params));
  voidOverlap_conf_params_init(params);  
  return params;
}

static void
free_string_field (char **s)
{
  if (*s)
    {
      free (*s);
      *s = 0;
    }
}


static void
voidOverlap_conf_release (struct voidOverlap_info *args_info)
{

  free_string_field (&(args_info->configFile_arg));
  free_string_field (&(args_info->configFile_orig));
  free_string_field (&(args_info->partFile1_arg));
  free_string_field (&(args_info->partFile1_orig));
  free_string_field (&(args_info->volFile1_arg));
  free_string_field (&(args_info->volFile1_orig));
  free_string_field (&(args_info->voidFile1_arg));
  free_string_field (&(args_info->voidFile1_orig));
  free_string_field (&(args_info->infoFile1_arg));
  free_string_field (&(args_info->infoFile1_orig));
  free_string_field (&(args_info->zoneFile1_arg));
  free_string_field (&(args_info->zoneFile1_orig));
  free_string_field (&(args_info->zonePartFile1_arg));
  free_string_field (&(args_info->zonePartFile1_orig));
  free_string_field (&(args_info->centerFile1_arg));
  free_string_field (&(args_info->centerFile1_orig));
  free_string_field (&(args_info->shapeFile1_arg));
  free_string_field (&(args_info->shapeFile1_orig));
  free_string_field (&(args_info->partFile2_arg));
  free_string_field (&(args_info->partFile2_orig));
  free_string_field (&(args_info->volFile2_arg));
  free_string_field (&(args_info->volFile2_orig));
  free_string_field (&(args_info->voidFile2_arg));
  free_string_field (&(args_info->voidFile2_orig));
  free_string_field (&(args_info->infoFile2_arg));
  free_string_field (&(args_info->infoFile2_orig));
  free_string_field (&(args_info->zoneFile2_arg));
  free_string_field (&(args_info->zoneFile2_orig));
  free_string_field (&(args_info->zonePartFile2_arg));
  free_string_field (&(args_info->zonePartFile2_orig));
  free_string_field (&(args_info->centerFile2_arg));
  free_string_field (&(args_info->centerFile2_orig));
  free_string_field (&(args_info->shapeFile2_arg));
  free_string_field (&(args_info->shapeFile2_orig));
  free_string_field (&(args_info->outfile_arg));
  free_string_field (&(args_info->outfile_orig));
  free_string_field (&(args_info->overlapFrac_orig));
  free_string_field (&(args_info->periodic_arg));
  free_string_field (&(args_info->periodic_orig));
  
  

  clear_given (args_info);
}


static void
write_into_file(FILE *outfile, const char *opt, const char *arg, const char *values[])
{
  FIX_UNUSED (values);
  if (arg) {
    fprintf(outfile, "%s=\"%s\"\n", opt, arg);
  } else {
    fprintf(outfile, "%s\n", opt);
  }
}


int
voidOverlap_conf_dump(FILE *outfile, struct voidOverlap_info *args_info)
{
  int i = 0;

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot dump options to stream\n", VOIDOVERLAP_CONF_PACKAGE);
      return EXIT_FAILURE;
    }

  if (args_info->help_given)
    write_into_file(outfile, "help", 0, 0 );
  if (args_info->version_given)
    write_into_file(outfile, "version", 0, 0 );
  if (args_info->configFile_given)
    write_into_file(outfile, "configFile", args_info->configFile_orig, 0);
  if (args_info->partFile1_given)
    write_into_file(outfile, "partFile1", args_info->partFile1_orig, 0);
  if (args_info->volFile1_given)
    write_into_file(outfile, "volFile1", args_info->volFile1_orig, 0);
  if (args_info->voidFile1_given)
    write_into_file(outfile, "voidFile1", args_info->voidFile1_orig, 0);
  if (args_info->infoFile1_given)
    write_into_file(outfile, "infoFile1", args_info->infoFile1_orig, 0);
  if (args_info->zoneFile1_given)
    write_into_file(outfile, "zoneFile1", args_info->zoneFile1_orig, 0);
  if (args_info->zonePartFile1_given)
    write_into_file(outfile, "zonePartFile1", args_info->zonePartFile1_orig, 0);
  if (args_info->centerFile1_given)
    write_into_file(outfile, "centerFile1", args_info->centerFile1_orig, 0);
  if (args_info->shapeFile1_given)
    write_into_file(outfile, "shapeFile1", args_info->shapeFile1_orig, 0);
  if (args_info->partFile2_given)
    write_into_file(outfile, "partFile2", args_info->partFile2_orig, 0);
  if (args_info->volFile2_given)
    write_into_file(outfile, "volFile2", args_info->volFile2_orig, 0);
  if (args_info->voidFile2_given)
    write_into_file(outfile, "voidFile2", args_info->voidFile2_orig, 0);
  if (args_info->infoFile2_given)
    write_into_file(outfile, "infoFile2", args_info->infoFile2_orig, 0);
  if (args_info->zoneFile2_given)
    write_into_file(outfile, "zoneFile2", args_info->zoneFile2_orig, 0);
  if (args_info->zonePartFile2_given)
    write_into_file(outfile, "zonePartFile2", args_info->zonePartFile2_orig, 0);
  if (args_info->centerFile2_given)
    write_into_file(outfile, "centerFile2", args_info->centerFile2_orig, 0);
  if (args_info->shapeFile2_given)
    write_into_file(outfile, "shapeFile2", args_info->shapeFile2_orig, 0);
  if (args_info->isObservation_given)
    write_into_file(outfile, "isObservation", 0, 0 );
  if (args_info->outfile_given)
    write_into_file(outfile, "outfile", args_info->outfile_orig, 0);
  if (args_info->useID_given)
    write_into_file(outfile, "useID", 0, 0 );
  if (args_info->overlapFrac_given)
    write_into_file(outfile, "overlapFrac", args_info->overlapFrac_orig, 0);
  if (args_info->periodic_given)
    write_into_file(outfile, "periodic", args_info->periodic_orig, 0);
  

  i = EXIT_SUCCESS;
  return i;
}

int
voidOverlap_conf_file_save(const char *filename, struct voidOverlap_info *args_info)
{
  FILE *outfile;
  int i = 0;

  outfile = fopen(filename, "w");

  if (!outfile)
    {
      fprintf (stderr, "%s: cannot open file for writing: %s\n", VOIDOVERLAP_CONF_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  i = voidOverlap_conf_dump(outfile, args_info);
  fclose (outfile);

  return i;
}

void
voidOverlap_conf_free (struct voidOverlap_info *args_info)
{
  voidOverlap_conf_release (args_info);
}

/** @brief replacement of strdup, which is not standard */
char *
gengetopt_strdup (const char *s)
{
  char *result = 0;
  if (!s)
    return result;

  result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}

int
voidOverlap_conf (int argc, char **argv, struct voidOverlap_info *args_info)
{
  return voidOverlap_conf2 (argc, argv, args_info, 0, 1, 1);
}

int
voidOverlap_conf_ext (int argc, char **argv, struct voidOverlap_info *args_info,
                   struct voidOverlap_conf_params *params)
{
  int result;
  result = voidOverlap_conf_internal (argc, argv, args_info, params, 0);

  if (result == EXIT_FAILURE)
    {
      voidOverlap_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
voidOverlap_conf2 (int argc, char **argv, struct voidOverlap_info *args_info, int override, int initialize, int check_required)
{
  int result;
  struct voidOverlap_conf_params params;
  
  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;

  result = voidOverlap_conf_internal (argc, argv, args_info, &params, 0);

  if (result == EXIT_FAILURE)
    {
      voidOverlap_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
voidOverlap_conf_required (struct voidOverlap_info *args_info, const char *prog_name)
{
  int result = EXIT_SUCCESS;

  if (voidOverlap_conf_required2(args_info, prog_name, 0) > 0)
    result = EXIT_FAILURE;

  if (result == EXIT_FAILURE)
    {
      voidOverlap_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}

int
voidOverlap_conf_required2 (struct voidOverlap_info *args_info, const char *prog_name, const char *additional_error)
{
  int error = 0;
  FIX_UNUSED (additional_error);

  /* checks for required options */
  if (! args_info->partFile1_given)
    {
      fprintf (stderr, "%s: '--partFile1' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->volFile1_given)
    {
      fprintf (stderr, "%s: '--volFile1' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->voidFile1_given)
    {
      fprintf (stderr, "%s: '--voidFile1' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->infoFile1_given)
    {
      fprintf (stderr, "%s: '--infoFile1' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->zoneFile1_given)
    {
      fprintf (stderr, "%s: '--zoneFile1' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->zonePartFile1_given)
    {
      fprintf (stderr, "%s: '--zonePartFile1' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->centerFile1_given)
    {
      fprintf (stderr, "%s: '--centerFile1' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->shapeFile1_given)
    {
      fprintf (stderr, "%s: '--shapeFile1' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->partFile2_given)
    {
      fprintf (stderr, "%s: '--partFile2' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->volFile2_given)
    {
      fprintf (stderr, "%s: '--volFile2' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->voidFile2_given)
    {
      fprintf (stderr, "%s: '--voidFile2' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->infoFile2_given)
    {
      fprintf (stderr, "%s: '--infoFile2' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->zoneFile2_given)
    {
      fprintf (stderr, "%s: '--zoneFile2' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->zonePartFile2_given)
    {
      fprintf (stderr, "%s: '--zonePartFile2' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->centerFile2_given)
    {
      fprintf (stderr, "%s: '--centerFile2' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->shapeFile2_given)
    {
      fprintf (stderr, "%s: '--shapeFile2' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  if (! args_info->outfile_given)
    {
      fprintf (stderr, "%s: '--outfile' option required%s\n", prog_name, (additional_error ? additional_error : ""));
      error = 1;
    }
  
  
  /* checks for dependences among options */

  return error;
}


static char *package_name = 0;

/**
 * @brief updates an option
 * @param field the generic pointer to the field to update
 * @param orig_field the pointer to the orig field
 * @param field_given the pointer to the number of occurrence of this option
 * @param prev_given the pointer to the number of occurrence already seen
 * @param value the argument for this option (if null no arg was specified)
 * @param possible_values the possible values for this option (if specified)
 * @param default_value the default value (in case the option only accepts fixed values)
 * @param arg_type the type of this option
 * @param check_ambiguity @see voidOverlap_conf_params.check_ambiguity
 * @param override @see voidOverlap_conf_params.override
 * @param no_free whether to free a possible previous value
 * @param multiple_option whether this is a multiple option
 * @param long_opt the corresponding long option
 * @param short_opt the corresponding short option (or '-' if none)
 * @param additional_error possible further error specification
 */
static
int update_arg(void *field, char **orig_field,
               unsigned int *field_given, unsigned int *prev_given, 
               char *value, const char *possible_values[],
               const char *default_value,
               voidOverlap_conf_arg_type arg_type,
               int check_ambiguity, int override,
               int no_free, int multiple_option,
               const char *long_opt, char short_opt,
               const char *additional_error)
{
  char *stop_char = 0;
  const char *val = value;
  int found;
  char **string_field;
  FIX_UNUSED (field);

  stop_char = 0;
  found = 0;

  if (!multiple_option && prev_given && (*prev_given || (check_ambiguity && *field_given)))
    {
      if (short_opt != '-')
        fprintf (stderr, "%s: `--%s' (`-%c') option given more than once%s\n", 
               package_name, long_opt, short_opt,
               (additional_error ? additional_error : ""));
      else
        fprintf (stderr, "%s: `--%s' option given more than once%s\n", 
               package_name, long_opt,
               (additional_error ? additional_error : ""));
      return 1; /* failure */
    }

  FIX_UNUSED (default_value);
    
  if (field_given && *field_given && ! override)
    return 0;
  if (prev_given)
    (*prev_given)++;
  if (field_given)
    (*field_given)++;
  if (possible_values)
    val = possible_values[found];

  switch(arg_type) {
  case ARG_FLAG:
    *((int *)field) = !*((int *)field);
    break;
  case ARG_DOUBLE:
    if (val) *((double *)field) = strtod (val, &stop_char);
    break;
  case ARG_STRING:
    if (val) {
      string_field = (char **)field;
      if (!no_free && *string_field)
        free (*string_field); /* free previous string */
      *string_field = gengetopt_strdup (val);
    }
    break;
  default:
    break;
  };

  /* check numeric conversion */
  switch(arg_type) {
  case ARG_DOUBLE:
    if (val && !(stop_char && *stop_char == '\0')) {
      fprintf(stderr, "%s: invalid numeric value: %s\n", package_name, val);
      return 1; /* failure */
    }
    break;
  default:
    ;
  };

  /* store the original value */
  switch(arg_type) {
  case ARG_NO:
  case ARG_FLAG:
    break;
  default:
    if (value && orig_field) {
      if (no_free) {
        *orig_field = value;
      } else {
        if (*orig_field)
          free (*orig_field); /* free previous string */
        *orig_field = gengetopt_strdup (value);
      }
    }
  };

  return 0; /* OK */
}


int
voidOverlap_conf_internal (
  int argc, char **argv, struct voidOverlap_info *args_info,
                        struct voidOverlap_conf_params *params, const char *additional_error)
{
  int c;	/* Character of the parsed option.  */

  int error = 0;
  struct voidOverlap_info local_args_info;
  
  int override;
  int initialize;
  int check_required;
  int check_ambiguity;
  
  package_name = argv[0];
  
  override = params->override;
  initialize = params->initialize;
  check_required = params->check_required;
  check_ambiguity = params->check_ambiguity;

  if (initialize)
    voidOverlap_conf_init (args_info);

  voidOverlap_conf_init (&local_args_info);

  optarg = 0;
  optind = 0;
  opterr = params->print_errors;
  optopt = '?';

  while (1)
    {
      int option_index = 0;

      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "configFile",	1, NULL, 0 },
        { "partFile1",	1, NULL, 0 },
        { "volFile1",	1, NULL, 0 },
        { "voidFile1",	1, NULL, 0 },
        { "infoFile1",	1, NULL, 0 },
        { "zoneFile1",	1, NULL, 0 },
        { "zonePartFile1",	1, NULL, 0 },
        { "centerFile1",	1, NULL, 0 },
        { "shapeFile1",	1, NULL, 0 },
        { "partFile2",	1, NULL, 0 },
        { "volFile2",	1, NULL, 0 },
        { "voidFile2",	1, NULL, 0 },
        { "infoFile2",	1, NULL, 0 },
        { "zoneFile2",	1, NULL, 0 },
        { "zonePartFile2",	1, NULL, 0 },
        { "centerFile2",	1, NULL, 0 },
        { "shapeFile2",	1, NULL, 0 },
        { "isObservation",	0, NULL, 0 },
        { "outfile",	1, NULL, 0 },
        { "useID",	0, NULL, 0 },
        { "overlapFrac",	1, NULL, 0 },
        { "periodic",	1, NULL, 0 },
        { 0,  0, 0, 0 }
      };

      c = getopt_long (argc, argv, "hV", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          voidOverlap_conf_print_help ();
          voidOverlap_conf_free (&local_args_info);
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          voidOverlap_conf_print_version ();
          voidOverlap_conf_free (&local_args_info);
          exit (EXIT_SUCCESS);


        case 0:	/* Long option with no short option */
          /* Configuration file path.  */
          if (strcmp (long_options[option_index].name, "configFile") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->configFile_arg), 
                 &(args_info->configFile_orig), &(args_info->configFile_given),
                &(local_args_info.configFile_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "configFile", '-',
                additional_error))
              goto failure;
          
          }
          /* Particle file for catalog 1.  */
          else if (strcmp (long_options[option_index].name, "partFile1") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->partFile1_arg), 
                 &(args_info->partFile1_orig), &(args_info->partFile1_given),
                &(local_args_info.partFile1_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "partFile1", '-',
                additional_error))
              goto failure;
          
          }
          /* Volume file for catalog 1.  */
          else if (strcmp (long_options[option_index].name, "volFile1") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->volFile1_arg), 
                 &(args_info->volFile1_orig), &(args_info->volFile1_given),
                &(local_args_info.volFile1_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "volFile1", '-',
                additional_error))
              goto failure;
          
          }
          /* Void info file for catalog 1.  */
          else if (strcmp (long_options[option_index].name, "voidFile1") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->voidFile1_arg), 
                 &(args_info->voidFile1_orig), &(args_info->voidFile1_given),
                &(local_args_info.voidFile1_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "voidFile1", '-',
                additional_error))
              goto failure;
          
          }
          /* Extra info file for catalog 1.  */
          else if (strcmp (long_options[option_index].name, "infoFile1") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->infoFile1_arg), 
                 &(args_info->infoFile1_orig), &(args_info->infoFile1_given),
                &(local_args_info.infoFile1_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "infoFile1", '-',
                additional_error))
              goto failure;
          
          }
          /* Zone file for catalog 1.  */
          else if (strcmp (long_options[option_index].name, "zoneFile1") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->zoneFile1_arg), 
                 &(args_info->zoneFile1_orig), &(args_info->zoneFile1_given),
                &(local_args_info.zoneFile1_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "zoneFile1", '-',
                additional_error))
              goto failure;
          
          }
          /* Zone-particle file for catalog 1.  */
          else if (strcmp (long_options[option_index].name, "zonePartFile1") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->zonePartFile1_arg), 
                 &(args_info->zonePartFile1_orig), &(args_info->zonePartFile1_given),
                &(local_args_info.zonePartFile1_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "zonePartFile1", '-',
                additional_error))
              goto failure;
          
          }
          /* Barycenter file for catalog 1.  */
          else if (strcmp (long_options[option_index].name, "centerFile1") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->centerFile1_arg), 
                 &(args_info->centerFile1_orig), &(args_info->centerFile1_given),
                &(local_args_info.centerFile1_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "centerFile1", '-',
                additional_error))
              goto failure;
          
          }
          /* Shape file for catalog 1.  */
          else if (strcmp (long_options[option_index].name, "shapeFile1") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->shapeFile1_arg), 
                 &(args_info->shapeFile1_orig), &(args_info->shapeFile1_given),
                &(local_args_info.shapeFile1_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "shapeFile1", '-',
                additional_error))
              goto failure;
          
          }
          /* Particle file for catalog 2.  */
          else if (strcmp (long_options[option_index].name, "partFile2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->partFile2_arg), 
                 &(args_info->partFile2_orig), &(args_info->partFile2_given),
                &(local_args_info.partFile2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "partFile2", '-',
                additional_error))
              goto failure;
          
          }
          /* Volume file for catalog 2.  */
          else if (strcmp (long_options[option_index].name, "volFile2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->volFile2_arg), 
                 &(args_info->volFile2_orig), &(args_info->volFile2_given),
                &(local_args_info.volFile2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "volFile2", '-',
                additional_error))
              goto failure;
          
          }
          /* Void info file for catalog 2.  */
          else if (strcmp (long_options[option_index].name, "voidFile2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->voidFile2_arg), 
                 &(args_info->voidFile2_orig), &(args_info->voidFile2_given),
                &(local_args_info.voidFile2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "voidFile2", '-',
                additional_error))
              goto failure;
          
          }
          /* Extra info file for catalog 2.  */
          else if (strcmp (long_options[option_index].name, "infoFile2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->infoFile2_arg), 
                 &(args_info->infoFile2_orig), &(args_info->infoFile2_given),
                &(local_args_info.infoFile2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "infoFile2", '-',
                additional_error))
              goto failure;
          
          }
          /* Zone file for catalog 2.  */
          else if (strcmp (long_options[option_index].name, "zoneFile2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->zoneFile2_arg), 
                 &(args_info->zoneFile2_orig), &(args_info->zoneFile2_given),
                &(local_args_info.zoneFile2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "zoneFile2", '-',
                additional_error))
              goto failure;
          
          }
          /* Zone-particle file for catalog 2.  */
          else if (strcmp (long_options[option_index].name, "zonePartFile2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->zonePartFile2_arg), 
                 &(args_info->zonePartFile2_orig), &(args_info->zonePartFile2_given),
                &(local_args_info.zonePartFile2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "zonePartFile2", '-',
                additional_error))
              goto failure;
          
          }
          /* Barycenter file for catalog 2.  */
          else if (strcmp (long_options[option_index].name, "centerFile2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->centerFile2_arg), 
                 &(args_info->centerFile2_orig), &(args_info->centerFile2_given),
                &(local_args_info.centerFile2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "centerFile2", '-',
                additional_error))
              goto failure;
          
          }
          /* Shape file for catalog 2.  */
          else if (strcmp (long_options[option_index].name, "shapeFile2") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->shapeFile2_arg), 
                 &(args_info->shapeFile2_orig), &(args_info->shapeFile2_given),
                &(local_args_info.shapeFile2_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "shapeFile2", '-',
                additional_error))
              goto failure;
          
          }
          /* We are working with observational data.  */
          else if (strcmp (long_options[option_index].name, "isObservation") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->isObservation_flag), 0, &(args_info->isObservation_given),
                &(local_args_info.isObservation_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "isObservation", '-',
                additional_error))
              goto failure;
          
          }
          /* Output file.  */
          else if (strcmp (long_options[option_index].name, "outfile") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->outfile_arg), 
                 &(args_info->outfile_orig), &(args_info->outfile_given),
                &(local_args_info.outfile_given), optarg, 0, 0, ARG_STRING,
                check_ambiguity, override, 0, 0,
                "outfile", '-',
                additional_error))
              goto failure;
          
          }
          /* Use unique catalog ID to match voids; otherwise use volumes.  */
          else if (strcmp (long_options[option_index].name, "useID") == 0)
          {
          
          
            if (update_arg((void *)&(args_info->useID_flag), 0, &(args_info->useID_given),
                &(local_args_info.useID_given), optarg, 0, 0, ARG_FLAG,
                check_ambiguity, override, 1, 0, "useID", '-',
                additional_error))
              goto failure;
          
          }
          /* threshold fraction of voronoi radius to count as matched.  */
          else if (strcmp (long_options[option_index].name, "overlapFrac") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->overlapFrac_arg), 
                 &(args_info->overlapFrac_orig), &(args_info->overlapFrac_given),
                &(local_args_info.overlapFrac_given), optarg, 0, "0.25", ARG_DOUBLE,
                check_ambiguity, override, 0, 0,
                "overlapFrac", '-',
                additional_error))
              goto failure;
          
          }
          /* Set of edges which are periodic.  */
          else if (strcmp (long_options[option_index].name, "periodic") == 0)
          {
          
          
            if (update_arg( (void *)&(args_info->periodic_arg), 
                 &(args_info->periodic_orig), &(args_info->periodic_given),
                &(local_args_info.periodic_given), optarg, 0, "xy", ARG_STRING,
                check_ambiguity, override, 0, 0,
                "periodic", '-',
                additional_error))
              goto failure;
          
          }
          
          break;
        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          goto failure;

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c%s\n", VOIDOVERLAP_CONF_PACKAGE, c, (additional_error ? additional_error : ""));
          abort ();
        } /* switch */
    } /* while */



  if (check_required)
    {
      error += voidOverlap_conf_required2 (args_info, argv[0], additional_error);
    }

  voidOverlap_conf_release (&local_args_info);

  if ( error )
    return (EXIT_FAILURE);

  return 0;

failure:
  
  voidOverlap_conf_release (&local_args_info);
  return (EXIT_FAILURE);
}

#ifndef CONFIG_FILE_LINE_SIZE
#define CONFIG_FILE_LINE_SIZE 2048
#endif
#define ADDITIONAL_ERROR " in configuration file "

#define CONFIG_FILE_LINE_BUFFER_SIZE (CONFIG_FILE_LINE_SIZE+3)
/* 3 is for "--" and "=" */

static int
_voidOverlap_conf_configfile (const char *filename, int *my_argc)
{
  FILE* file;
  char my_argv[CONFIG_FILE_LINE_BUFFER_SIZE+1];
  char linebuf[CONFIG_FILE_LINE_SIZE];
  int line_num = 0;
  int result = 0, equal;
  char *fopt, *farg;
  char *str_index;
  size_t len, next_token;
  char delimiter;

  if ((file = fopen(filename, "r")) == 0)
    {
      fprintf (stderr, "%s: Error opening configuration file '%s'\n",
               VOIDOVERLAP_CONF_PACKAGE, filename);
      return EXIT_FAILURE;
    }

  while ((fgets(linebuf, CONFIG_FILE_LINE_SIZE, file)) != 0)
    {
      ++line_num;
      my_argv[0] = '\0';
      len = strlen(linebuf);
      if (len > (CONFIG_FILE_LINE_BUFFER_SIZE-1))
        {
          fprintf (stderr, "%s:%s:%d: Line too long in configuration file\n",
                   VOIDOVERLAP_CONF_PACKAGE, filename, line_num);
          result = EXIT_FAILURE;
          break;
        }

      /* find first non-whitespace character in the line */
      next_token = strspn (linebuf, " \t\r\n");
      str_index  = linebuf + next_token;

      if ( str_index[0] == '\0' || str_index[0] == '#')
        continue; /* empty line or comment line is skipped */

      fopt = str_index;

      /* truncate fopt at the end of the first non-valid character */
      next_token = strcspn (fopt, " \t\r\n=");

      if (fopt[next_token] == '\0') /* the line is over */
        {
          farg  = 0;
          equal = 0;
          goto noarg;
        }

      /* remember if equal sign is present */
      equal = (fopt[next_token] == '=');
      fopt[next_token++] = '\0';

      /* advance pointers to the next token after the end of fopt */
      next_token += strspn (fopt + next_token, " \t\r\n");

      /* check for the presence of equal sign, and if so, skip it */
      if ( !equal )
        if ((equal = (fopt[next_token] == '=')))
          {
            next_token++;
            next_token += strspn (fopt + next_token, " \t\r\n");
          }
      str_index  += next_token;

      /* find argument */
      farg = str_index;
      if ( farg[0] == '\"' || farg[0] == '\'' )
        { /* quoted argument */
          str_index = strchr (++farg, str_index[0] ); /* skip opening quote */
          if (! str_index)
            {
              fprintf
                (stderr,
                 "%s:%s:%d: unterminated string in configuration file\n",
                 VOIDOVERLAP_CONF_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }
      else
        { /* read up the remaining part up to a delimiter */
          next_token = strcspn (farg, " \t\r\n#\'\"");
          str_index += next_token;
        }

      /* truncate farg at the delimiter and store it for further check */
      delimiter = *str_index, *str_index++ = '\0';

      /* everything but comment is illegal at the end of line */
      if (delimiter != '\0' && delimiter != '#')
        {
          str_index += strspn(str_index, " \t\r\n");
          if (*str_index != '\0' && *str_index != '#')
            {
              fprintf
                (stderr,
                 "%s:%s:%d: malformed string in configuration file\n",
                 VOIDOVERLAP_CONF_PACKAGE, filename, line_num);
              result = EXIT_FAILURE;
              break;
            }
        }

    noarg:
      if (!strcmp(fopt,"include")) {
        if (farg && *farg) {
          result = _voidOverlap_conf_configfile(farg, my_argc);
        } else {
          fprintf(stderr, "%s:%s:%d: include requires a filename argument.\n",
                  VOIDOVERLAP_CONF_PACKAGE, filename, line_num);
        }
        continue;
      }
      len = strlen(fopt);
      strcat (my_argv, len > 1 ? "--" : "-");
      strcat (my_argv, fopt);
      if (len > 1 && ((farg && *farg) || equal))
        strcat (my_argv, "=");
      if (farg && *farg)
        strcat (my_argv, farg);
      ++(*my_argc);

      cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
      cmd_line_list_tmp->next = cmd_line_list;
      cmd_line_list = cmd_line_list_tmp;
      cmd_line_list->string_arg = gengetopt_strdup(my_argv);
    } /* while */

  if (file)
    fclose(file);
  return result;
}

int
voidOverlap_conf_configfile (
  const char *filename,
                           struct voidOverlap_info *args_info,
                           int override, int initialize, int check_required)
{
  struct voidOverlap_conf_params params;

  params.override = override;
  params.initialize = initialize;
  params.check_required = check_required;
  params.check_ambiguity = 0;
  params.print_errors = 1;
  
  return voidOverlap_conf_config_file (filename, args_info, &params);
}

int
voidOverlap_conf_config_file (const char *filename,
                           struct voidOverlap_info *args_info,
                           struct voidOverlap_conf_params *params)
{
  int i, result;
  int my_argc = 1;
  char **my_argv_arg;
  char *additional_error;

  /* store the program name */
  cmd_line_list_tmp = (struct line_list *) malloc (sizeof (struct line_list));
  cmd_line_list_tmp->next = cmd_line_list;
  cmd_line_list = cmd_line_list_tmp;
  cmd_line_list->string_arg = gengetopt_strdup (VOIDOVERLAP_CONF_PACKAGE);

  result = _voidOverlap_conf_configfile(filename, &my_argc);

  if (result != EXIT_FAILURE) {
    my_argv_arg = (char **) malloc((my_argc+1) * sizeof(char *));
    cmd_line_list_tmp = cmd_line_list;

    for (i = my_argc - 1; i >= 0; --i) {
      my_argv_arg[i] = cmd_line_list_tmp->string_arg;
      cmd_line_list_tmp = cmd_line_list_tmp->next;
    }

    my_argv_arg[my_argc] = 0;

    additional_error = (char *)malloc(strlen(filename) + strlen(ADDITIONAL_ERROR) + 1);
    strcpy (additional_error, ADDITIONAL_ERROR);
    strcat (additional_error, filename);
    result =
      voidOverlap_conf_internal (my_argc, my_argv_arg, args_info,
                              params,
                              additional_error);

    free (additional_error);
    free (my_argv_arg);
  }

  free_cmd_list();
  if (result == EXIT_FAILURE)
    {
      voidOverlap_conf_free (args_info);
      exit (EXIT_FAILURE);
    }
  
  return result;
}
